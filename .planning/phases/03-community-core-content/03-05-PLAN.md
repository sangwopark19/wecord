---
phase: 03-community-core-content
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(tabs)/community.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
autonomous: true
gap_closure: true
requirements:
  - COMM-01

must_haves:
  truths:
    - "Community tab is visible in the bottom tab bar alongside Home"
    - "Tapping the Community tab navigates to the community search screen"
  artifacts:
    - path: "apps/mobile/app/(tabs)/community.tsx"
      provides: "Proxy route file that Expo Router resolves for the community tab"
      contains: "Redirect"
  key_links:
    - from: "apps/mobile/app/(tabs)/_layout.tsx"
      to: "apps/mobile/app/(tabs)/community.tsx"
      via: "Expo Router file-based routing (Tabs.Screen name='community' matches community.tsx)"
      pattern: "name=\"community\""
    - from: "apps/mobile/app/(tabs)/community.tsx"
      to: "apps/mobile/app/(community)/search.tsx"
      via: "Redirect component from expo-router"
      pattern: "Redirect.*community.*search"
---

<objective>
Fix the community tab not appearing in the bottom tab bar.

Purpose: The UAT blocker — "community tab is not visible" — blocks ALL 16 UAT tests. The root cause is that `Tabs.Screen name="community"` has no matching route file in the `(tabs)/` directory. Expo Router silently drops tabs without a backing file.

Output: A proxy route file at `(tabs)/community.tsx` that makes the tab visible and redirects to the existing `(community)/search` screen.
</objective>

<execution_context>
@/Users/sangwopark19/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/community-tab-not-visible.md

Root cause documented in debug session: Expo Router requires each Tabs.Screen name to match a file in the (tabs)/ directory. The `community` tab has no backing file — only `_layout.tsx` and `index.tsx` exist in (tabs)/.

Current (tabs)/_layout.tsx defines:
```tsx
<Tabs.Screen
  name="community"
  options={{
    title: 'Community',
    tabBarIcon: ({ color, size }) => (
      <Ionicons name="people-outline" size={size} color={color} />
    ),
    href: '/(community)/search' as never,
  }}
/>
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create community tab proxy and fix tab layout</name>
  <files>apps/mobile/app/(tabs)/community.tsx, apps/mobile/app/(tabs)/_layout.tsx</files>
  <action>
1. Create `apps/mobile/app/(tabs)/community.tsx`:
   - Import `Redirect` from `expo-router`
   - Export a default component that returns `<Redirect href="/(community)/search" />`
   - This file exists solely so Expo Router resolves `Tabs.Screen name="community"` to a real route

2. Update `apps/mobile/app/(tabs)/_layout.tsx`:
   - Remove the `href: '/(community)/search' as never` from the community Tabs.Screen options — the proxy file handles navigation now, and `href` is not needed when a matching file exists
   - Keep the title, icon, and all other options unchanged

Do NOT move any community screens into (tabs)/ — keep the existing (community) route group as-is. The proxy approach preserves the current routing architecture.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/wecord-wv && test -f apps/mobile/app/\(tabs\)/community.tsx && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    - `apps/mobile/app/(tabs)/community.tsx` exists and exports a Redirect component
    - `apps/mobile/app/(tabs)/_layout.tsx` Tabs.Screen name="community" no longer has href override
    - TypeScript compiles without errors
    - Community tab will now appear in the bottom tab bar (Expo Router finds matching file)
  </done>
</task>

</tasks>

<verification>
1. File exists: `apps/mobile/app/(tabs)/community.tsx`
2. TypeScript compiles: `npx tsc --noEmit` passes
3. Tab layout has community screen without href override
</verification>

<success_criteria>
- Community tab proxy file exists at `apps/mobile/app/(tabs)/community.tsx`
- The file uses `Redirect` from expo-router to navigate to `/(community)/search`
- TypeScript compiles cleanly
- The (tabs)/_layout.tsx community screen no longer uses `href` override
</success_criteria>

<output>
After completion, create `.planning/phases/03-community-core-content/03-05-SUMMARY.md`
</output>
