---
phase: 03-community-core-content
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(community)/search.tsx
  - apps/mobile/components/community/CommunityCard.tsx
  - apps/mobile/components/community/CommunityPreviewSheet.tsx
autonomous: true
gap_closure: true
requirements: [COMM-01, COMM-02]

must_haves:
  truths:
    - "Community search results display in a 2-column grid on both web and mobile"
    - "Community cards show a placeholder when cover_image_url is null (no black rectangles)"
    - "Artist avatar shows a person-outline icon fallback when profile_image_url is null"
  artifacts:
    - path: "apps/mobile/app/(community)/search.tsx"
      provides: "2-column grid layout with width-constrained renderItem"
      contains: "width: '50%'"
    - path: "apps/mobile/components/community/CommunityCard.tsx"
      provides: "Cover image with null-guard fallback"
      contains: "cover_image_url"
    - path: "apps/mobile/components/community/CommunityPreviewSheet.tsx"
      provides: "Artist avatar with person-outline fallback"
      contains: "person-outline"
  key_links:
    - from: "apps/mobile/app/(community)/search.tsx"
      to: "apps/mobile/components/community/CommunityCard.tsx"
      via: "renderItem renders CommunityCard inside width-constrained View"
      pattern: "width.*50%"
---

<objective>
Fix community search grid layout (2-column) and image fallbacks (cover image + artist avatar) identified in UAT tests 2 and 3.

Purpose: UAT revealed black rectangles on web (null cover image rendered by expo-image), 1-column layout on mobile (missing width constraint), and invisible artist avatars in preview sheet (no fallback for null profile_image_url). These are visual bugs that degrade the community discovery experience.

Output: Corrected search grid and image fallback rendering across all platforms.
</objective>

<execution_context>
@/Users/sangwopark19/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/community-search-grid-layout.md
@.planning/debug/artist-avatar-fallback-missing.md

<interfaces>
<!-- Existing patterns to follow -->

From apps/mobile/components/community/ArtistMemberScroll.tsx (avatar fallback pattern):
```tsx
{item.profile_image_url ? (
  <Image
    source={{ uri: item.profile_image_url }}
    style={{ width: 56, height: 56 }}
    contentFit="cover"
  />
) : (
  <View
    style={{ width: 56, height: 56 }}
    className="bg-card items-center justify-center"
  >
    <Ionicons name="person-outline" size={24} color="#999999" />
  </View>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 2-column grid layout and cover image fallback</name>
  <files>apps/mobile/app/(community)/search.tsx, apps/mobile/components/community/CommunityCard.tsx</files>
  <action>
**search.tsx:**
1. Change renderItem wrapper from `<View className="flex-1">` to `<View style={{ width: '50%' }}>`. This gives FlatList numColumns=2 the explicit width constraint it needs.
2. Remove the dead `useDebounce` hook function (lines 17-28). It uses useCallback instead of useEffect and is never called. The manual setTimeout debounce in handleQueryChange works but leaks timers — replace with a proper useEffect-based debounce:
   - Remove the inline setTimeout from handleQueryChange (just call setQuery)
   - Add a useEffect that watches `query` and sets a 300ms timeout to update `debouncedQuery`, with cleanup returning clearTimeout
3. Remove the unused `useCallback` import (only keep useState, useEffect).

**CommunityCard.tsx:**
1. Replace the unconditional `<Image source={{ uri: community.cover_image_url ?? undefined }}>` with a ternary:
   - If `community.cover_image_url` exists: render the Image as-is
   - If null: render a `<View style={{ width: '100%', aspectRatio: 1 }} className="bg-card items-center justify-center">` with an `<Ionicons name="people-outline" size={32} color="#666666" />` placeholder
2. Add Ionicons import: `import { Ionicons } from '@expo/vector-icons';`
3. Remove `flex-1` from the Pressable className (the parent View now controls width). Keep `m-2 bg-card rounded-xl overflow-hidden`.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/wecord-wv && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>FlatList renders 2-column grid with explicit 50% width per item. Null cover images show a placeholder icon instead of black rectangles. Dead useDebounce code removed, debounce works via useEffect.</done>
</task>

<task type="auto">
  <name>Task 2: Fix artist avatar fallback in CommunityPreviewSheet</name>
  <files>apps/mobile/components/community/CommunityPreviewSheet.tsx</files>
  <action>
1. Add Ionicons import: `import { Ionicons } from '@expo/vector-icons';`
2. In the artistMembers.map section (around line 108-117), replace the unconditional Image with the same ternary pattern used in ArtistMemberScroll.tsx:
   - If `artist.profile_image_url` exists: render `<Image source={{ uri: artist.profile_image_url }} style={{ width: 48, height: 48, borderRadius: 24 }} contentFit="cover" />`
   - If null: render `<View style={{ width: 48, height: 48, borderRadius: 24 }} className="bg-card items-center justify-center"><Ionicons name="person-outline" size={20} color="#999999" /></View>`
3. Also apply the same null-guard to the cover image at line 72-77: if `community.cover_image_url` is null, render a placeholder View with `style={{ width: '100%', aspectRatio: 16/9 }}` and `className="bg-card"` instead of expo-image with undefined URI.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/wecord-wv && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>Artist avatars with null profile_image_url show a person-outline icon fallback. Cover image with null URL shows a dark placeholder instead of black rectangle.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project apps/mobile/tsconfig.json` passes
- search.tsx renderItem uses `style={{ width: '50%' }}`
- CommunityCard.tsx has conditional image rendering with fallback
- CommunityPreviewSheet.tsx has conditional rendering for both cover image and artist avatars
- No `{ uri: undefined }` passed to expo-image in any modified file
</verification>

<success_criteria>
- 2-column grid layout works on both web and mobile (width: 50% on renderItem wrapper)
- No black rectangles when cover_image_url is null (placeholder shown instead)
- Artist avatars show person-outline icon when profile_image_url is null
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-community-core-content/03-06-SUMMARY.md`
</output>
