---
phase: 04-highlights-notices-notifications-translation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/mobile/tests/useHighlight.test.ts
  - apps/mobile/tests/useNotifications.test.ts
  - apps/mobile/tests/useUnreadNotificationCount.test.ts
  - apps/mobile/tests/useNotices.test.ts
  - apps/mobile/tests/useTranslate.test.ts
autonomous: true
requirements:
  - HIGH-01
  - NOTF-06
  - NOTF-08
  - NOTC-05
  - TRAN-01

must_haves:
  truths:
    - "All 5 Wave 0 test stubs exist and are runnable by vitest"
    - "Each stub contains at least one describe block with a skipped or pending test"
    - "pnpm --filter mobile test --run exits 0"
  artifacts:
    - path: "apps/mobile/tests/useHighlight.test.ts"
      provides: "Test stub for highlight hook"
      contains: "describe"
    - path: "apps/mobile/tests/useNotifications.test.ts"
      provides: "Test stub for notifications hook"
      contains: "describe"
    - path: "apps/mobile/tests/useUnreadNotificationCount.test.ts"
      provides: "Test stub for unread count hook"
      contains: "describe"
    - path: "apps/mobile/tests/useNotices.test.ts"
      provides: "Test stub for notices hook"
      contains: "describe"
    - path: "apps/mobile/tests/useTranslate.test.ts"
      provides: "Test stub for translate hook"
      contains: "describe"
  key_links: []
---

<objective>
Create Wave 0 test stub files for all Phase 4 hooks so that per-task automated verification commands have valid test targets.

Purpose: Nyquist compliance requires every task's `<verify>` to have an automated command that can run. These stubs ensure vitest can find and run test files referenced in plans 01-04.
Output: 5 test stub files in apps/mobile/tests/ matching vitest's include pattern.
</objective>

<execution_context>
@/Users/sangwopark19/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/mobile/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 5 test stub files for Phase 4 hooks</name>
  <files>
    apps/mobile/tests/useHighlight.test.ts,
    apps/mobile/tests/useNotifications.test.ts,
    apps/mobile/tests/useUnreadNotificationCount.test.ts,
    apps/mobile/tests/useNotices.test.ts,
    apps/mobile/tests/useTranslate.test.ts
  </files>
  <read_first>
    apps/mobile/vitest.config.ts,
    apps/mobile/tests/setup.ts,
    apps/mobile/tests/community.test.ts
  </read_first>
  <action>
Create 5 test stub files in `apps/mobile/tests/` following the existing test file pattern (e.g., `community.test.ts`). The vitest config `include` pattern is `['tests/**/*.test.ts', 'tests/**/*.test.tsx']`, so files MUST be in the `tests/` directory.

Each file should contain a `describe` block with `it.todo()` tests that document expected behaviors. These will be implemented during task execution.

**1. apps/mobile/tests/useHighlight.test.ts:**
```typescript
import { describe, it } from 'vitest';

describe('useHighlight', () => {
  it.todo('returns highlight data for a community');
  it.todo('returns notices, creatorPosts, fanPosts, artistMembers sections');
  it.todo('is disabled when communityId is empty');
  it.todo('handles error from highlight Edge Function');
});
```

**2. apps/mobile/tests/useNotifications.test.ts:**
```typescript
import { describe, it } from 'vitest';

describe('useNotifications', () => {
  it.todo('returns notifications for a community');
  it.todo('orders by created_at descending');
  it.todo('is disabled when communityId is empty');
});

describe('useNotificationPreferences', () => {
  it.todo('returns preference toggles for a community');
  it.todo('upserts preference changes');
});
```

**3. apps/mobile/tests/useUnreadNotificationCount.test.ts:**
```typescript
import { describe, it } from 'vitest';

describe('useUnreadNotificationCount', () => {
  it.todo('returns initial unread count from query');
  it.todo('increments count on Realtime INSERT event');
  it.todo('cleans up Realtime channel on unmount');
});
```

**4. apps/mobile/tests/useNotices.test.ts:**
```typescript
import { describe, it } from 'vitest';

describe('useNotices', () => {
  it.todo('returns published notices for a community');
  it.todo('orders pinned first, then by published_at');
  it.todo('is disabled when communityId is empty');
});

describe('useNoticeDetail', () => {
  it.todo('returns single notice by id');
});
```

**5. apps/mobile/tests/useTranslate.test.ts:**
```typescript
import { describe, it } from 'vitest';

describe('useTranslate', () => {
  it.todo('fetches translation on first translate call');
  it.todo('toggles isTranslated off without refetch');
  it.todo('re-shows from memory on second translate call');
  it.todo('sets error state on Edge Function failure');
  it.todo('uses preferred_language from profile');
});
```
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/wecord-wv && pnpm --filter mobile test --run 2>&1 | tail -10</automated>
  </verify>
  <done>All 5 test stub files exist in apps/mobile/tests/, vitest discovers them, and `pnpm --filter mobile test --run` exits 0 with all stubs shown as todo/pending</done>
</task>

</tasks>

<verification>
- `pnpm --filter mobile test --run` passes (stubs are todo, not failing)
- All 5 files exist in apps/mobile/tests/
</verification>

<success_criteria>
- 5 test stub files created and recognized by vitest
- Test run exits 0
- Nyquist compliance: every plan's verify commands now have valid test targets
</success_criteria>

<output>
After completion, create `.planning/phases/04-highlights-notices-notifications-translation/04-00-SUMMARY.md`
</output>
