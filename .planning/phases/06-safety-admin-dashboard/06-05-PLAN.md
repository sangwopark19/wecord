---
phase: 06-safety-admin-dashboard
plan: 05
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - apps/admin/app/(dashboard)/communities/page.tsx
  - apps/admin/app/(dashboard)/communities/[id]/page.tsx
  - apps/admin/app/(dashboard)/creators/page.tsx
  - apps/admin/app/(dashboard)/creators/[id]/page.tsx
  - apps/admin/app/(dashboard)/members/page.tsx
  - apps/admin/app/(dashboard)/banners/page.tsx
autonomous: true
requirements:
  - ADMN-01
  - ADMN-02
  - ADMN-03
  - ADMN-04
  - ADMN-10

must_haves:
  truths:
    - "Admin can create, edit, and delete communities"
    - "Admin can create and manage creator accounts"
    - "Admin can register and manage artist members per community"
    - "Admin can view community member list and statistics"
    - "Admin can create, edit, and delete promotion banners"
  artifacts:
    - path: "apps/admin/app/(dashboard)/communities/page.tsx"
      provides: "Community CRUD list page"
      contains: "Communities"
    - path: "apps/admin/app/(dashboard)/creators/page.tsx"
      provides: "Creator management page"
      contains: "Creators"
    - path: "apps/admin/app/(dashboard)/members/page.tsx"
      provides: "Member list and artist member management"
      contains: "Members"
    - path: "apps/admin/app/(dashboard)/banners/page.tsx"
      provides: "Promotion banner CRUD"
      contains: "Banners"
  key_links:
    - from: "apps/admin/app/(dashboard)/communities/page.tsx"
      to: "communities table"
      via: "supabaseAdmin CRUD"
      pattern: "supabaseAdmin.*communities"
    - from: "apps/admin/app/(dashboard)/banners/page.tsx"
      to: "promotion_banners table"
      via: "supabaseAdmin CRUD"
      pattern: "supabaseAdmin.*promotion_banners"
---

<objective>
Admin CRUD pages for communities, creators, members, and promotion banners.

Purpose: Provide the admin with full management capability over communities (ADMN-01), creator accounts (ADMN-02), artist members (ADMN-03), member statistics (ADMN-04), and promotion banners (ADMN-10).

Output: 4 fully functional admin pages following the established notices CRUD pattern (Table + AlertDialog + supabaseAdmin).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-safety-admin-dashboard/06-CONTEXT.md
@.planning/phases/06-safety-admin-dashboard/06-RESEARCH.md
@.planning/phases/06-safety-admin-dashboard/06-UI-SPEC.md
@.planning/phases/06-safety-admin-dashboard/06-02-SUMMARY.md

<interfaces>
<!-- From existing codebase -->

From apps/admin/app/(dashboard)/notices/page.tsx (CRUD pattern):
```typescript
// 'use client', useState, useEffect
// supabaseAdmin.from('table').select().order()
// Table/TableHeader/TableBody/TableRow/TableCell/TableHead
// Badge for status, Button for actions
// AlertDialog for delete confirmation
// handleDelete: supabaseAdmin.from('table').delete().eq('id', id)
```

Database tables (from packages/db/src/schema/):
```
communities: id, name, slug, description, avatar_url, type ('solo'|'group'), member_count, created_at
community_members: id, community_id, user_id, role ('fan'|'creator'|'admin'), community_nickname, created_at
artist_members: id, community_id, user_id, display_name, avatar_url, bio, created_at
profiles: id, user_id, nickname, avatar_url, bio, language, date_of_birth, created_at
promotion_banners: id, title, image_url, link_url, is_active, display_order, created_at
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Communities CRUD + Creators management pages</name>
  <files>
    apps/admin/app/(dashboard)/communities/page.tsx,
    apps/admin/app/(dashboard)/communities/[id]/page.tsx,
    apps/admin/app/(dashboard)/creators/page.tsx,
    apps/admin/app/(dashboard)/creators/[id]/page.tsx
  </files>
  <read_first>
    apps/admin/app/(dashboard)/notices/page.tsx,
    apps/admin/app/(dashboard)/notices/new/page.tsx,
    apps/admin/app/(dashboard)/notices/[id]/page.tsx,
    apps/admin/lib/supabase.ts,
    packages/db/src/schema/community.ts,
    packages/db/src/schema/artist-member.ts,
    apps/admin/components/ui/table.tsx,
    apps/admin/components/ui/input.tsx,
    apps/admin/components/ui/select.tsx,
    .planning/phases/06-safety-admin-dashboard/06-UI-SPEC.md
  </read_first>
  <action>
    **A) Communities list page** — `apps/admin/app/(dashboard)/communities/page.tsx` ('use client'):
    - Heading: "Communities" (20px/600)
    - "Create Community" button (accent, top-right, per UI-SPEC copywriting)
    - Table columns: Name | Slug | Type (solo/group badge) | Members | Created | Actions (Edit/Delete)
    - Fetch: `supabaseAdmin.from('communities').select('*').order('created_at', { ascending: false })`
    - Delete: AlertDialog confirmation -> `supabaseAdmin.from('communities').delete().eq('id', id)`
    - Edit: Link to `/communities/[id]`
    - Create: inline form at top (toggleable) or modal — fields:
      - Name (Input, required)
      - Slug (Input, auto-generate from name with lowercase + hyphen)
      - Description (Textarea)
      - Type (Select: solo / group)
      - Avatar URL (Input, optional)
    - On create: `supabaseAdmin.from('communities').insert({ name, slug, description, type, avatar_url })`

    **B) Community edit page** — `apps/admin/app/(dashboard)/communities/[id]/page.tsx` ('use client'):
    - Heading: "Edit Community" with community name
    - Form fields: Name, Slug, Description, Type, Avatar URL (pre-filled from fetch)
    - "Save Changes" button (accent) and "Discard Changes" button (outline) per UI-SPEC
    - On save: `supabaseAdmin.from('communities').update({ ... }).eq('id', id)`
    - Back link to /communities

    **C) Creators list page** — `apps/admin/app/(dashboard)/creators/page.tsx` ('use client'):
    - Heading: "Creators" (20px/600)
    - "Add Creator" button (accent, top-right, per UI-SPEC copywriting)
    - Table columns: Nickname | Email | Communities | Role | Created | Actions
    - Fetch creators: `supabaseAdmin.from('community_members').select('*, profiles(nickname, avatar_url)').eq('role', 'creator')` — join with profiles to show global nickname
    - Also fetch community names for display
    - Add creator flow: form with:
      - User search (Input — search profiles by nickname or email)
      - Community select (Select — pick community to assign creator role)
      - On add: `supabaseAdmin.from('community_members').update({ role: 'creator' }).eq('user_id', userId).eq('community_id', communityId)`
      - If user is not a member yet, insert into community_members with role='creator'
    - Remove creator: change role back to 'fan': `supabaseAdmin.from('community_members').update({ role: 'fan' }).eq('id', memberId)`

    **D) Creator edit page** — `apps/admin/app/(dashboard)/creators/[id]/page.tsx` ('use client'):
    - Shows creator profile details (from community_members + profiles join)
    - Community assignments list with ability to add/remove creator role per community
    - Artist member registrations for this creator (list from artist_members table)
    - "Register as Artist Member" button to add to artist_members table for a community
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/wecord-wv && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - apps/admin/app/(dashboard)/communities/page.tsx contains "Communities"
    - apps/admin/app/(dashboard)/communities/page.tsx contains "Create Community"
    - apps/admin/app/(dashboard)/communities/page.tsx contains "supabaseAdmin"
    - apps/admin/app/(dashboard)/communities/page.tsx contains "communities"
    - apps/admin/app/(dashboard)/communities/[id]/page.tsx contains "Save Changes"
    - apps/admin/app/(dashboard)/creators/page.tsx contains "Creators"
    - apps/admin/app/(dashboard)/creators/page.tsx contains "Add Creator"
    - apps/admin/app/(dashboard)/creators/page.tsx contains "community_members"
    - apps/admin/app/(dashboard)/creators/[id]/page.tsx contains "artist_members"
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>Communities page with full CRUD (create/edit/delete). Creators page with role management and artist member registration. Both follow established notices CRUD pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Members page + Banners CRUD page</name>
  <files>
    apps/admin/app/(dashboard)/members/page.tsx,
    apps/admin/app/(dashboard)/banners/page.tsx
  </files>
  <read_first>
    apps/admin/app/(dashboard)/notices/page.tsx,
    apps/admin/app/(dashboard)/communities/page.tsx,
    apps/admin/lib/supabase.ts,
    packages/db/src/schema/community.ts,
    packages/db/src/schema/promotion-banner.ts,
    .planning/phases/06-safety-admin-dashboard/06-UI-SPEC.md
  </read_first>
  <action>
    **A) Members page** — `apps/admin/app/(dashboard)/members/page.tsx` ('use client'):
    - Heading: "Members" (20px/600)
    - Community filter: Select dropdown at top to filter by community (default: all)
    - Search: Input field to search by nickname
    - Table columns: Nickname | Community | Role (badge: fan/creator/admin) | Joined | Post Count | Actions
    - Fetch: `supabaseAdmin.from('community_members').select('*, profiles(nickname, avatar_url)').order('created_at', { ascending: false }).limit(50)`
    - If community selected: add `.eq('community_id', selectedCommunity)`
    - Statistics summary above table (per ADMN-04):
      - Total members count
      - Members per community (if specific community selected: member count, creator count, fan count)
      - Active members (posted in last 7 days)
    - Artist member management: for group communities, show "Register Artist Member" button per member row (for creator-role members)
      - On click: insert into `artist_members` table with display_name from community_nickname
    - No delete — members cannot be removed (they leave voluntarily). Admin can only change roles.

    **B) Banners CRUD page** — `apps/admin/app/(dashboard)/banners/page.tsx` ('use client'):
    - Heading: "Banners" (20px/600)
    - "Create Banner" button (accent, top-right, per UI-SPEC copywriting)
    - Table columns: Image Preview (small thumbnail) | Title | Link URL | Active (Switch toggle) | Order | Actions (Edit/Delete)
    - Fetch: `supabaseAdmin.from('promotion_banners').select('*').order('display_order', { ascending: true })`
    - Create/Edit inline form (or modal):
      - Title (Input, required)
      - Image URL (Input, required — direct URL, no upload in MVP)
      - Link URL (Input, required — where banner links to)
      - Display Order (Input type number)
      - Is Active (Switch toggle)
    - On create: `supabaseAdmin.from('promotion_banners').insert({ title, image_url, link_url, display_order, is_active })`
    - On edit: `supabaseAdmin.from('promotion_banners').update({ ... }).eq('id', id)`
    - Toggle active: `supabaseAdmin.from('promotion_banners').update({ is_active: !current }).eq('id', id)` — use Switch component with immediate update
    - Delete: AlertDialog confirmation -> `supabaseAdmin.from('promotion_banners').delete().eq('id', id)`
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/wecord-wv && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - apps/admin/app/(dashboard)/members/page.tsx contains "Members"
    - apps/admin/app/(dashboard)/members/page.tsx contains "community_members"
    - apps/admin/app/(dashboard)/members/page.tsx contains "artist_members"
    - apps/admin/app/(dashboard)/members/page.tsx contains "supabaseAdmin"
    - apps/admin/app/(dashboard)/banners/page.tsx contains "Banners"
    - apps/admin/app/(dashboard)/banners/page.tsx contains "Create Banner"
    - apps/admin/app/(dashboard)/banners/page.tsx contains "promotion_banners"
    - apps/admin/app/(dashboard)/banners/page.tsx contains "is_active"
    - apps/admin/app/(dashboard)/banners/page.tsx contains "Switch"
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>Members page with community filter, search, statistics, and artist member registration. Banners page with full CRUD and active toggle. Both follow established admin CRUD pattern.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes
- All 4 CRUD pages render with data from supabaseAdmin
- Communities: create/edit/delete works
- Creators: role assignment and artist member registration works
- Members: filter by community, search by nickname, statistics visible
- Banners: create/edit/delete/toggle-active works
</verification>

<success_criteria>
- Admin can create/edit/delete communities (ADMN-01)
- Admin can create/manage creator accounts (ADMN-02)
- Admin can register/manage artist members (ADMN-03)
- Admin can view member list and statistics (ADMN-04)
- Admin can create/edit/delete promotion banners (ADMN-10)
</success_criteria>

<output>
After completion, create `.planning/phases/06-safety-admin-dashboard/06-05-SUMMARY.md`
</output>
