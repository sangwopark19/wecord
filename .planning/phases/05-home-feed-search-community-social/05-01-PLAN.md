---
phase: 05-home-feed-search-community-social
plan: 01
type: execute
wave: 1
depends_on:
  - 05-00
files_modified:
  - packages/supabase/functions/home-feed/index.ts
  - apps/mobile/hooks/home/useHomeFeed.ts
  - apps/mobile/hooks/home/usePromotionBanners.ts
  - apps/mobile/hooks/notification/useAllUnreadNotificationCount.ts
  - apps/mobile/components/home/PromotionBannerCarousel.tsx
  - apps/mobile/components/home/CommunityChip.tsx
  - apps/mobile/components/home/RecommendationSection.tsx
  - apps/mobile/components/home/HomeSearchBar.tsx
  - apps/mobile/components/post/PostCard.tsx
  - apps/mobile/app/(tabs)/index.tsx
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05

must_haves:
  truths:
    - "User with 0 communities sees 2-column creator recommendation grid with search bar and promotion banner"
    - "User with 1+ communities sees unified cross-community feed with community chips on each post"
    - "Home header shows Wecord logo and notification bell with all-communities unread badge"
    - "Promotion banner carousel auto-scrolls every 3 seconds with dot indicator"
    - "Tapping community chip navigates to that community"
  artifacts:
    - path: "packages/supabase/functions/home-feed/index.ts"
      provides: "Cross-community feed merge Edge Function"
      contains: "posts_with_nickname"
    - path: "apps/mobile/app/(tabs)/index.tsx"
      provides: "Home tab screen (conditional 0-community vs 1+-community)"
      min_lines: 50
    - path: "apps/mobile/components/home/PromotionBannerCarousel.tsx"
      provides: "Auto-scroll banner carousel with dot indicator"
      contains: "setInterval"
    - path: "apps/mobile/components/home/CommunityChip.tsx"
      provides: "Community source chip for unified feed"
      contains: "border-teal"
    - path: "apps/mobile/hooks/home/useHomeFeed.ts"
      provides: "Cross-community feed hook with useInfiniteQuery"
      contains: "useInfiniteQuery"
  key_links:
    - from: "apps/mobile/app/(tabs)/index.tsx"
      to: "apps/mobile/hooks/home/useHomeFeed.ts"
      via: "useHomeFeed hook call"
      pattern: "useHomeFeed"
    - from: "apps/mobile/hooks/home/useHomeFeed.ts"
      to: "packages/supabase/functions/home-feed/index.ts"
      via: "supabase.functions.invoke('home-feed')"
      pattern: "functions\\.invoke.*home-feed"
    - from: "apps/mobile/app/(tabs)/index.tsx"
      to: "apps/mobile/components/home/PromotionBannerCarousel.tsx"
      via: "import and render"
      pattern: "PromotionBannerCarousel"
    - from: "apps/mobile/components/post/PostCard.tsx"
      to: "apps/mobile/components/home/CommunityChip.tsx"
      via: "conditional render when showCommunityChip is true"
      pattern: "showCommunityChip"
---

<objective>
Home feed Edge Function + Home tab screen with conditional views (0-community recommendation vs 1+-community unified feed), promotion banner carousel, and notification bell.

Purpose: Replace the placeholder Home tab with the real home experience that drives retention — returning users see a unified feed, new users see curated recommendations.
Output: home-feed Edge Function, Home tab screen with two views, PromotionBannerCarousel, CommunityChip, HomeSearchBar, RecommendationSection components, all-communities notification count hook.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-home-feed-search-community-social/05-CONTEXT.md
@.planning/phases/05-home-feed-search-community-social/05-RESEARCH.md
@.planning/phases/05-home-feed-search-community-social/05-UI-SPEC.md
@.planning/phases/05-home-feed-search-community-social/05-00-SUMMARY.md

<interfaces>
From apps/mobile/hooks/post/useFanFeed.ts:
```typescript
export interface PostWithNickname {
  id: string;
  community_id: string;
  author_id: string;
  artist_member_id: string | null;
  author_role: 'fan' | 'creator';
  content: string;
  content_rating: string | null;
  media_urls: string[] | null;
  post_type: 'text' | 'image' | 'video';
  like_count: number;
  comment_count: number;
  created_at: string;
  author_nickname: string;
  author_cm_id: string;
  artist_member_name: string | null;
  community_name: string;
  community_slug: string;
  isLiked: boolean;
}
```

From apps/mobile/components/notification/NotificationBellBadge.tsx:
```typescript
interface NotificationBellBadgeProps {
  communityId: string;
  onPress: () => void;
}
// Uses useUnreadNotificationCount(userId, communityId)
```

From apps/mobile/hooks/notification/useUnreadNotificationCount.ts:
```typescript
export function useUnreadNotificationCount(userId: string, communityId: string): number
// Queries notifications with .eq('community_id', communityId).eq('is_read', false)
```

From apps/mobile/components/community/CommunityCard.tsx:
```typescript
interface CommunityCardProps {
  community: CommunitySearchResult;
}
// Uses: useCommunityMember(community.id) for navigation routing
// Layout: Image (aspect 1:1) + name + member_count
```

From apps/mobile/components/post/PostCard.tsx:
```typescript
interface PostCardProps {
  post: PostWithNickname;
  onLike?: () => void;
  onDelete?: () => void;
  clampLines?: number;
  communityId?: string;
}
```

From packages/supabase/functions/highlight/index.ts (Edge Function boilerplate):
```typescript
// Pattern: Deno.serve, corsHeaders, createClient with Authorization header, POST body parsing
const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
};
Deno.serve(async (req: Request) => { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: home-feed Edge Function + useHomeFeed hook + usePromotionBanners hook + useAllUnreadNotificationCount hook</name>
  <files>
    packages/supabase/functions/home-feed/index.ts
    apps/mobile/hooks/home/useHomeFeed.ts
    apps/mobile/hooks/home/usePromotionBanners.ts
    apps/mobile/hooks/notification/useAllUnreadNotificationCount.ts
  </files>
  <read_first>
    packages/supabase/functions/highlight/index.ts
    apps/mobile/hooks/post/useFanFeed.ts
    apps/mobile/hooks/notification/useUnreadNotificationCount.ts
    apps/mobile/lib/supabase.ts
    apps/mobile/stores/authStore.ts
  </read_first>
  <action>
1. Create `packages/supabase/functions/home-feed/index.ts` Edge Function:
   - Use `Deno.serve()` pattern (same as highlight/index.ts)
   - Handle CORS OPTIONS preflight with corsHeaders
   - Accept POST body: `{ cursor?: { createdAt: string; id: string }, limit?: number }`
   - Create user-context Supabase client using Authorization header
   - Step 1: Get caller's community_ids via `supabase.from('community_members').select('community_id').eq('user_id', user.id)`
   - If communityIds is empty, return `{ posts: [], nextCursor: null, isEmpty: true }`
   - Step 2: Single merged query: `supabase.from('posts_with_nickname').select('*').in('community_id', communityIds).order('created_at', { ascending: false }).order('id', { ascending: false }).limit(limit ?? 15)`
   - Apply cursor: `.or(\`created_at.lt.${cursor.createdAt},and(created_at.eq.${cursor.createdAt},id.lt.${cursor.id})\`)`
   - Return `{ posts, nextCursor: lastPost ? { createdAt: lastPost.created_at, id: lastPost.id } : null, isEmpty: false }`
   - Error handling: return 500 with `{ error: String(error) }`

2. Create `apps/mobile/hooks/home/useHomeFeed.ts`:
   - Import `useInfiniteQuery` from `@tanstack/react-query`
   - Import `supabase` from `../../lib/supabase`
   - Import `useAuthStore` from `../../stores/authStore`
   - Import `PostWithNickname` from `../post/useFanFeed`
   - Export `useMyMemberships(userId)` — `useQuery` that fetches `community_members.select('community_id').eq('user_id', userId)`, returns `string[]`
   - Export `useHomeFeed()`:
     - Get `user` from `useAuthStore()`
     - Call `useMyMemberships(user?.id)`
     - `isNewUser = memberships?.length === 0`
     - If not new user: `useInfiniteQuery` with `queryKey: ['homeFeed']`
     - `queryFn`: call `supabase.functions.invoke('home-feed', { body: { cursor, limit: 15 } })` — parse response as `HomeFeedResponse`
     - Then batch-fetch liked post IDs (reuse `fetchLikedPostIds` from useFanFeed.ts or inline same logic)
     - Map posts with `isLiked` flag
     - `getNextPageParam`: if lastPage.length < 15, return undefined; else return `{ createdAt: last.created_at, id: last.id }`
     - Return `{ ...infiniteQuery, isNewUser, memberships }`

3. Create `apps/mobile/hooks/home/usePromotionBanners.ts`:
   - `useQuery` with `queryKey: ['promotionBanners']`
   - `queryFn`: `supabase.from('promotion_banners').select('*').eq('is_active', true).order('sort_order', { ascending: true })`
   - Return type: `{ id: string; image_url: string; link_url: string; sort_order: number }[]`

4. Create `apps/mobile/hooks/notification/useAllUnreadNotificationCount.ts`:
   - Copy pattern from `useUnreadNotificationCount.ts` but:
     - Remove `communityId` parameter — query `notifications` with only `.eq('user_id', userId).eq('is_read', false)` (no community_id filter)
     - Realtime subscription filter: only `user_id=eq.${userId}` (no community filter)
     - Channel name: `user:${userId}:notifications:all`
   - Export: `useAllUnreadNotificationCount(userId: string): number`
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/wecord-wv && cat packages/supabase/functions/home-feed/index.ts | grep -c "Deno.serve" && cat apps/mobile/hooks/home/useHomeFeed.ts | grep -c "useInfiniteQuery" && cat apps/mobile/hooks/home/usePromotionBanners.ts | grep -c "promotion_banners" && cat apps/mobile/hooks/notification/useAllUnreadNotificationCount.ts | grep -c "useAllUnreadNotificationCount"</automated>
  </verify>
  <acceptance_criteria>
    - packages/supabase/functions/home-feed/index.ts contains "Deno.serve" and "posts_with_nickname" and "community_members" and "corsHeaders"
    - packages/supabase/functions/home-feed/index.ts contains "in('community_id', communityIds)" for single-query merge (no N+1)
    - packages/supabase/functions/home-feed/index.ts contains compound cursor: "created_at.lt" and "id.lt"
    - apps/mobile/hooks/home/useHomeFeed.ts contains "useInfiniteQuery" and "functions.invoke" and "home-feed"
    - apps/mobile/hooks/home/useHomeFeed.ts exports "useMyMemberships" and "useHomeFeed"
    - apps/mobile/hooks/home/usePromotionBanners.ts contains "promotion_banners" and "is_active" and "sort_order"
    - apps/mobile/hooks/notification/useAllUnreadNotificationCount.ts exports "useAllUnreadNotificationCount"
    - apps/mobile/hooks/notification/useAllUnreadNotificationCount.ts does NOT contain ".eq('community_id'"
  </acceptance_criteria>
  <done>home-feed Edge Function handles cross-community merge query; useHomeFeed uses useInfiniteQuery with cursor pagination; usePromotionBanners queries active banners; useAllUnreadNotificationCount provides cross-community unread count</done>
</task>

<task type="auto">
  <name>Task 2: Home tab screen + PromotionBannerCarousel + CommunityChip + HomeSearchBar + RecommendationSection + PostCard extension</name>
  <files>
    apps/mobile/components/home/PromotionBannerCarousel.tsx
    apps/mobile/components/home/CommunityChip.tsx
    apps/mobile/components/home/RecommendationSection.tsx
    apps/mobile/components/home/HomeSearchBar.tsx
    apps/mobile/components/post/PostCard.tsx
    apps/mobile/app/(tabs)/index.tsx
  </files>
  <read_first>
    apps/mobile/app/(tabs)/index.tsx
    apps/mobile/components/post/PostCard.tsx
    apps/mobile/components/community/CommunityCard.tsx
    apps/mobile/components/notification/NotificationBellBadge.tsx
    apps/mobile/app/(community)/[id]/fan.tsx
    apps/mobile/hooks/home/useHomeFeed.ts
    apps/mobile/hooks/home/usePromotionBanners.ts
    apps/mobile/hooks/notification/useAllUnreadNotificationCount.ts
    .planning/phases/05-home-feed-search-community-social/05-UI-SPEC.md
  </read_first>
  <action>
1. Create `apps/mobile/components/home/PromotionBannerCarousel.tsx`:
   - Props: `banners: { id: string; image_url: string; link_url: string }[]`
   - If `!banners || banners.length === 0` return `null`
   - FlatList with `horizontal pagingEnabled`, `showsHorizontalScrollIndicator={false}`
   - Container: `width: '100%'`, `aspectRatio: 16/9`
   - Each item: `expo-image` with `contentFit="cover"` `transition={200}`. Guard undefined image with ternary — render placeholder `<View className="bg-input" />` when image_url is falsy.
   - Auto-scroll: `useRef<NodeJS.Timeout>` for interval, `useRef<FlatList>` for list
     - `startAutoScroll()`: `setInterval(() => { setCurrentIndex(prev => (prev + 1) % banners.length); flatListRef.current?.scrollToIndex({ index: next, animated: true }); }, 3000)`
     - `onScrollBeginDrag`: clear interval (pause auto-scroll)
     - `onScrollEndDrag`: restart auto-scroll
     - `useEffect` cleanup: `clearInterval(intervalRef.current)` on unmount
     - If banners.length === 1: no auto-scroll, no dot indicator
   - Dot indicator: row of 6px circles below image, active `bg-teal`, inactive `bg-input`, gap 4px
   - `onViewableItemsChanged` to update `currentIndex` from scroll
   - Tap handler: `router.push(banner.link_url as never)` — deeplink to community, external, or in-app route
   - `accessibilityLabel` on each banner image

2. Create `apps/mobile/components/home/CommunityChip.tsx`:
   - Props: `communityId: string; communityName: string; communitySlug?: string`
   - Layout: `flex-row items-center gap-1, border border-teal rounded-full px-2 py-0.5 mb-2`
   - Pressable with `accessibilityRole="link"`
   - Show `Ionicons people-outline size={12} color="#00E5C3"` + `Text text-label text-teal` with `communityName`
   - onPress: `router.push(\`/(community)/${communityId}\` as never)`

3. Create `apps/mobile/components/home/RecommendationSection.tsx`:
   - Uses `useCommunitySearch('')` (empty query = all communities) or a dedicated `useRecommendedCommunities` that queries `supabase.from('communities').select('*').limit(20).order('member_count', { ascending: false })`
   - Render: section header "home.recommendation.heading" text via `useTranslation('home')` with `text-heading font-semibold text-foreground px-4`
   - FlatList with `numColumns={2}`, each item wrapped in `<View style={{ width: '50%' }}>` (Phase 3 grid pattern)
   - Each item: `<CommunityCard community={item} />`

4. Create `apps/mobile/components/home/HomeSearchBar.tsx`:
   - Props: none
   - Pressable wrapper (not TextInput — non-editable, tappable)
   - Layout: `mx-4 h-11 bg-input rounded-xl flex-row items-center px-4`
   - Content: `Ionicons search-outline size={20} color="#666666"` + `Text text-body text-subtle ml-2` placeholder text (use `useTranslation('community')` for search placeholder or hardcode "Search")
   - onPress: `router.push('/(community)/search' as never)` — navigates to existing Phase 3 search screen
   - `accessibilityRole="search"`

5. Extend `apps/mobile/components/post/PostCard.tsx`:
   - Add `showCommunityChip?: boolean` prop to `PostCardProps`
   - When `showCommunityChip` is true, render `<CommunityChip communityId={post.community_id} communityName={post.community_name} />` above the existing header row (nickname + avatar)
   - Import `CommunityChip` from `../home/CommunityChip`

6. Replace `apps/mobile/app/(tabs)/index.tsx` entirely:
   - SafeAreaView bg-background
   - Header: View flex-row justify-between items-center px-4 pt-2
     - Left: `<Text className="text-display font-semibold text-foreground">Wecord</Text>`
     - Right: import `NotificationBellBadge` but use `useAllUnreadNotificationCount` instead. Create inline wrapper:
       ```
       const { user } = useAuthStore();
       const count = useAllUnreadNotificationCount(user?.id ?? '');
       ```
       Render bell icon with badge using same visual as NotificationBellBadge but with all-communities count. Or create a `HomeNotificationBell` wrapper that renders the bell icon + badge View using the all-count hook. onPress: `router.push('/(community)/notifications' as never)` — or a general notifications route.
   - Conditional body:
     - If `isNewUser` (from `useHomeFeed`):
       - `<HomeSearchBar />`
       - `<PromotionBannerCarousel banners={banners} />`
       - `<RecommendationSection />`
       - Wrap all in `ScrollView` with `RefreshControl tintColor="#00E5C3"`
     - If not new user (1+ communities):
       - `FlashList` (import from `@shopify/flash-list`)
       - `ListHeaderComponent`: `<PromotionBannerCarousel banners={banners} />`
       - Data: `homeFeed.data?.pages.flat() ?? []`
       - `renderItem`: `<PostCard post={item} showCommunityChip communityId={item.community_id} />`
       - `onEndReached`: `homeFeed.fetchNextPage()`
       - `onEndReachedThreshold={0.5}`
       - `estimatedItemSize={200}`
       - `RefreshControl tintColor="#00E5C3" onRefresh={homeFeed.refetch}`
   - Error state: Show `home.error` text with retry
   - Import `usePromotionBanners` for `banners` data
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/wecord-wv && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/(tabs)/index.tsx contains "useHomeFeed" and "isNewUser" and "FlashList" and "PromotionBannerCarousel"
    - apps/mobile/app/(tabs)/index.tsx contains "Wecord" (logo text) and "useAllUnreadNotificationCount"
    - apps/mobile/app/(tabs)/index.tsx contains "RefreshControl" with "tintColor" of "#00E5C3"
    - apps/mobile/components/home/PromotionBannerCarousel.tsx contains "setInterval" and "clearInterval" and "pagingEnabled" and "onScrollBeginDrag"
    - apps/mobile/components/home/PromotionBannerCarousel.tsx contains "banners.length === 0" returning null
    - apps/mobile/components/home/CommunityChip.tsx contains "border-teal" and "communityName" and "router.push"
    - apps/mobile/components/home/RecommendationSection.tsx contains "numColumns={2}" and "CommunityCard"
    - apps/mobile/components/home/HomeSearchBar.tsx contains "/(community)/search" and "search-outline"
    - apps/mobile/components/post/PostCard.tsx contains "showCommunityChip" and "CommunityChip"
    - TypeScript compilation passes (npx tsc --noEmit exits 0 or only pre-existing errors)
  </acceptance_criteria>
  <done>Home tab shows 0-community recommendation view (search bar + banner + 2-col grid) and 1+-community unified feed (banner + PostCard with community chips); header shows Wecord logo and notification bell with all-communities badge; carousel auto-scrolls every 3s with dot indicator</done>
</task>

</tasks>

<verification>
- `pnpm --filter mobile test --run` passes (existing tests not broken)
- `npx tsc --noEmit --project apps/mobile/tsconfig.json` passes
- Home tab placeholder is replaced with functional screen
- home-feed Edge Function file exists and is ready for deployment
</verification>

<success_criteria>
- HOME-01: 0-community view renders RecommendationSection with CommunityCard grid
- HOME-02: 1+-community view renders unified FlashList feed via home-feed Edge Function
- HOME-03: Each post in unified feed shows CommunityChip with community name, tappable to navigate
- HOME-04: Home header shows "Wecord" logo text + NotificationBellBadge with all-communities count
- HOME-05: PromotionBannerCarousel renders active banners with auto-scroll, dot indicator, deeplink tap
</success_criteria>

<output>
After completion, create `.planning/phases/05-home-feed-search-community-social/05-01-SUMMARY.md`
</output>
