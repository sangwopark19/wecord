---
phase: 05-home-feed-search-community-social
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/mobile/app/(community)/[id]/post-search.tsx
  - apps/mobile/app/(community)/[id]/profile/[memberId].tsx
  - apps/mobile/app/(community)/[id]/profile/[memberId]/followers.tsx
  - apps/mobile/app/(community)/[id]/profile/[memberId]/following.tsx
  - apps/mobile/components/search/HighlightedText.tsx
  - apps/mobile/components/community/FollowButton.tsx
  - apps/mobile/hooks/search/usePostSearch.ts
  - apps/mobile/hooks/community/useFollowMember.ts
  - apps/mobile/hooks/community/useCommunityProfile.ts
  - apps/mobile/hooks/community/useFollowerList.ts
  - apps/mobile/hooks/community/useFollowingList.ts
  - apps/mobile/app/(community)/[id]/fan.tsx
  - apps/mobile/app/(community)/[id]/artist.tsx
  - apps/mobile/components/post/PostCard.tsx
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - FLLW-01
  - FLLW-02
  - FLLW-03

must_haves:
  truths:
    - "User can search posts within a community by keyword using full-text search"
    - "Search results show keyword highlighted in Teal (#00E5C3)"
    - "User can tap an avatar/nickname in PostCard to visit that member's community profile"
    - "Community profile shows nickname, post count, follower count, following count"
    - "User can follow/unfollow another member from their community profile page"
    - "User can view follower and following lists from the profile page"
    - "Fan and Artist tabs show search icon for in-community post search"
  artifacts:
    - path: "apps/mobile/app/(community)/[id]/post-search.tsx"
      provides: "In-community post full-text search screen"
      contains: "usePostSearch"
    - path: "apps/mobile/components/search/HighlightedText.tsx"
      provides: "Keyword highlight component"
      contains: "#00E5C3"
    - path: "apps/mobile/app/(community)/[id]/profile/[memberId].tsx"
      provides: "Community profile screen with stats and posts/comments tabs"
      contains: "useCommunityProfile"
    - path: "apps/mobile/components/community/FollowButton.tsx"
      provides: "Follow/Unfollow toggle button"
      contains: "useFollowMember"
    - path: "apps/mobile/hooks/community/useFollowMember.ts"
      provides: "Follow/unfollow mutation"
      contains: "community_follows"
    - path: "apps/mobile/hooks/search/usePostSearch.ts"
      provides: "Debounced full-text search hook"
      contains: "textSearch"
  key_links:
    - from: "apps/mobile/app/(community)/[id]/post-search.tsx"
      to: "apps/mobile/hooks/search/usePostSearch.ts"
      via: "usePostSearch hook"
      pattern: "usePostSearch"
    - from: "apps/mobile/hooks/search/usePostSearch.ts"
      to: "posts_with_nickname"
      via: "supabase textSearch query"
      pattern: "textSearch.*content"
    - from: "apps/mobile/components/community/FollowButton.tsx"
      to: "apps/mobile/hooks/community/useFollowMember.ts"
      via: "useFollowMember hook"
      pattern: "useFollowMember"
    - from: "apps/mobile/hooks/community/useFollowMember.ts"
      to: "community_follows"
      via: "supabase insert/delete"
      pattern: "community_follows"
    - from: "apps/mobile/components/post/PostCard.tsx"
      to: "apps/mobile/app/(community)/[id]/profile/[memberId].tsx"
      via: "router.push on avatar/nickname tap"
      pattern: "profile.*memberId"
---

<objective>
In-community post search with keyword highlighting + community follow/unfollow social graph with profile and list screens.

Purpose: Complete the social layer — users can find content within communities and connect with other members via follow/unfollow, profile viewing, and follower/following lists.
Output: PostSearchScreen, HighlightedText, CommunityProfileScreen, FollowerListScreen, FollowingListScreen, FollowButton, and all supporting hooks. Fan/Artist tabs get search icon. PostCard gets avatar/nickname tap navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-home-feed-search-community-social/05-CONTEXT.md
@.planning/phases/05-home-feed-search-community-social/05-RESEARCH.md
@.planning/phases/05-home-feed-search-community-social/05-UI-SPEC.md
@.planning/phases/05-home-feed-search-community-social/05-01-SUMMARY.md

<interfaces>
From apps/mobile/hooks/post/useFanFeed.ts:
```typescript
export interface PostWithNickname {
  id: string;
  community_id: string;
  author_id: string;
  artist_member_id: string | null;
  author_role: 'fan' | 'creator';
  content: string;
  content_rating: string | null;
  media_urls: string[] | null;
  post_type: 'text' | 'image' | 'video';
  like_count: number;
  comment_count: number;
  created_at: string;
  author_nickname: string;
  author_cm_id: string;
  artist_member_name: string | null;
  community_name: string;
  community_slug: string;
  isLiked: boolean;
}
```

From packages/db/src/schema/follow.ts:
```typescript
export const communityFollows = pgTable('community_follows', {
  id: uuid('id').defaultRandom().primaryKey(),
  followerCmId: uuid('follower_cm_id').notNull().references(() => communityMembers.id),
  followingCmId: uuid('following_cm_id').notNull().references(() => communityMembers.id),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
});
// RLS: community_follows_insert_own checks cm1.user_id = (select auth.uid()) AND same community
// RLS: community_follows_delete_own checks cm.user_id = (select auth.uid())
```

From apps/mobile/hooks/community/useCommunityMember.ts (inferred):
```typescript
// Returns membership data including: id (cm_id), community_id, user_id, community_nickname, avatar_url
export function useCommunityMember(communityId: string): UseQueryResult<CommunityMember | null>
```

From apps/mobile/components/post/PostCard.tsx:
```typescript
interface PostCardProps {
  post: PostWithNickname;
  onLike?: () => void;
  onDelete?: () => void;
  clampLines?: number;
  communityId?: string;
  showCommunityChip?: boolean; // Added in 05-01
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search hooks + HighlightedText + PostSearchScreen + Fan/Artist tab search icon</name>
  <files>
    apps/mobile/hooks/search/usePostSearch.ts
    apps/mobile/components/search/HighlightedText.tsx
    apps/mobile/app/(community)/[id]/post-search.tsx
    apps/mobile/app/(community)/[id]/fan.tsx
    apps/mobile/app/(community)/[id]/artist.tsx
  </files>
  <read_first>
    apps/mobile/app/(community)/[id]/fan.tsx
    apps/mobile/app/(community)/[id]/artist.tsx
    apps/mobile/hooks/post/useFanFeed.ts
    apps/mobile/components/post/PostCard.tsx
    apps/mobile/lib/supabase.ts
    .planning/phases/05-home-feed-search-community-social/05-UI-SPEC.md
  </read_first>
  <action>
1. Create `apps/mobile/hooks/search/usePostSearch.ts`:
   - Import `useQuery` from `@tanstack/react-query`, `supabase` from `../../lib/supabase`, `useState`, `useEffect`
   - Internal state: `rawQuery` (from TextInput), `debouncedQuery` (300ms debounce via useEffect + setTimeout/clearTimeout pattern — per Phase 3 established pattern)
   - Export `usePostSearch(communityId: string)`:
     - Returns `{ query, setQuery, debouncedQuery, ...queryResult }`
     - `setQuery` sets `rawQuery`
     - useEffect debounce: `const timer = setTimeout(() => setDebouncedQuery(rawQuery), 300); return () => clearTimeout(timer);`
     - `useQuery` with `queryKey: ['postSearch', communityId, debouncedQuery]`
     - `queryFn`:
       ```typescript
       if (!debouncedQuery.trim()) return [];
       const { data, error } = await supabase
         .from('posts_with_nickname')
         .select('*')
         .eq('community_id', communityId)
         .textSearch('content', debouncedQuery, { type: 'websearch', config: 'simple' })
         .order('created_at', { ascending: false })
         .limit(20);
       if (error) throw error;
       return (data ?? []) as PostWithNickname[];
       ```
     - `enabled: !!debouncedQuery.trim()`
     - Import `PostWithNickname` from `../post/useFanFeed`

2. Create `apps/mobile/components/search/HighlightedText.tsx`:
   ```typescript
   import { Text, TextStyle } from 'react-native';

   interface HighlightedTextProps {
     text: string;
     query: string;
     style?: TextStyle;
     highlightColor?: string;
   }

   export function HighlightedText({ text, query, style, highlightColor = '#00E5C3' }: HighlightedTextProps) {
     if (!query.trim()) return <Text style={style}>{text}</Text>;

     const escaped = query.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
     const regex = new RegExp(`(${escaped})`, 'gi');
     const parts = text.split(regex);

     return (
       <Text style={style}>
         {parts.map((part, i) =>
           regex.test(part)
             ? <Text key={i} style={{ color: highlightColor }}>{part}</Text>
             : <Text key={i}>{part}</Text>
         )}
       </Text>
     );
   }
   ```
   IMPORTANT: Reset regex lastIndex before each test() call, or use a different matching approach. The `regex.test(part)` with `gi` flag advances lastIndex and can produce incorrect alternation. Safer approach: compare `part.toLowerCase() === query.toLowerCase()` for the split-based pattern. Or use `new RegExp(escaped, 'i')` (without `g` flag) for the test.

3. Create `apps/mobile/app/(community)/[id]/post-search.tsx`:
   - SafeAreaView bg-background
   - Header: Back button (Ionicons `chevron-back` 24px, touch 44x44) + TextInput (flex-1, h-11, bg-input, rounded-xl, px-4, text-body text-foreground, autoFocus)
     - TextInput placeholder: `t('postSearch.placeholder')` from `useTranslation('community')`
     - `placeholderTextColor="#666666"`
     - `value={query}` `onChangeText={setQuery}` from `usePostSearch(id)`
   - Body: FlashList of results
     - `renderItem`: render `PostCard` but with content text replaced by `HighlightedText` — or pass `highlightQuery` prop to PostCard. Simpler: render PostCard as-is with content, and below content add `HighlightedText` preview. Actually simplest: render PostCard normally (existing component), and the highlight is visible because the search matched. For MVP, the PostCard shows the post and the match is in the content. To show keyword highlighting per SRCH-03: Add optional `highlightQuery?: string` prop to PostCard. When provided, render content using `HighlightedText` instead of plain `Text`. Update PostCard to accept this prop.
   - Empty state: when `debouncedQuery.trim()` and results empty — show `postSearch.empty.heading` + `postSearch.empty.body`
   - Error state: show `postSearch.error` + retry pressable `postSearch.error.retry`
   - Use `useLocalSearchParams<{ id: string }>()` for communityId

4. Add search icon to `apps/mobile/app/(community)/[id]/fan.tsx`:
   - In the header/top area (above or next to SortFilterChipBar), add a Pressable with `Ionicons search-outline size={24} color="#999999"` at top-right
   - Touch target: `style={{ width: 44, height: 44, alignItems: 'center', justifyContent: 'center' }}`
   - `accessibilityLabel={t('search.accessibilityLabel')}` — uses `community` namespace
   - onPress: `router.push(\`/(community)/${id}/post-search\` as never)`

5. Add same search icon to `apps/mobile/app/(community)/[id]/artist.tsx`:
   - Same pattern as fan.tsx — search icon in header area
   - Same navigation target: `/(community)/${id}/post-search`

6. Update `apps/mobile/components/post/PostCard.tsx`:
   - Add `highlightQuery?: string` prop to PostCardProps
   - In the content rendering area, when `highlightQuery` is truthy, render `<HighlightedText text={displayContent} query={highlightQuery} style={contentStyle} />` instead of plain `<Text>` for the post content
   - Import `HighlightedText` from `../search/HighlightedText`
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/wecord-wv && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/hooks/search/usePostSearch.ts contains "textSearch" and "websearch" and "debouncedQuery" and "setTimeout"
    - apps/mobile/hooks/search/usePostSearch.ts contains "posts_with_nickname" and "community_id"
    - apps/mobile/components/search/HighlightedText.tsx contains "#00E5C3" and "split(regex)"
    - apps/mobile/app/(community)/[id]/post-search.tsx contains "usePostSearch" and "HighlightedText" or "highlightQuery"
    - apps/mobile/app/(community)/[id]/post-search.tsx contains "postSearch.placeholder" and "postSearch.empty"
    - apps/mobile/app/(community)/[id]/fan.tsx contains "search-outline" and "post-search"
    - apps/mobile/app/(community)/[id]/artist.tsx contains "search-outline" and "post-search"
    - apps/mobile/components/post/PostCard.tsx contains "highlightQuery" prop
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>In-community post search works with debounced full-text query and keyword highlighting; Fan and Artist tabs show search icon navigating to post-search screen; PostCard supports highlightQuery prop</done>
</task>

<task type="auto">
  <name>Task 2: Follow hooks + FollowButton + CommunityProfile + Follower/Following lists + PostCard avatar navigation</name>
  <files>
    apps/mobile/hooks/community/useFollowMember.ts
    apps/mobile/hooks/community/useCommunityProfile.ts
    apps/mobile/hooks/community/useFollowerList.ts
    apps/mobile/hooks/community/useFollowingList.ts
    apps/mobile/components/community/FollowButton.tsx
    apps/mobile/app/(community)/[id]/profile/[memberId].tsx
    apps/mobile/app/(community)/[id]/profile/[memberId]/followers.tsx
    apps/mobile/app/(community)/[id]/profile/[memberId]/following.tsx
    apps/mobile/components/post/PostCard.tsx
  </files>
  <read_first>
    packages/db/src/schema/follow.ts
    apps/mobile/hooks/community/useCommunityMember.ts
    apps/mobile/hooks/post/useFanFeed.ts
    apps/mobile/components/post/PostCard.tsx
    apps/mobile/components/community/LeaveConfirmDialog.tsx
    apps/mobile/lib/supabase.ts
    apps/mobile/stores/authStore.ts
    .planning/phases/05-home-feed-search-community-social/05-UI-SPEC.md
  </read_first>
  <action>
1. Create `apps/mobile/hooks/community/useFollowMember.ts`:
   - Import `useMutation`, `useQueryClient` from `@tanstack/react-query`
   - Import `supabase` from `../../lib/supabase`
   - Export `useFollowMember()`:
     - `queryClient = useQueryClient()`
     - `useMutation` with `mutationFn: async ({ followerCmId, followingCmId, isFollowing }: { followerCmId: string; followingCmId: string; isFollowing: boolean }) =>`
       - If `isFollowing`: DELETE from `community_follows` where `follower_cm_id = followerCmId AND following_cm_id = followingCmId`
       - If `!isFollowing`: INSERT into `community_follows` with `{ follower_cm_id: followerCmId, following_cm_id: followingCmId }`
       - If error, throw error
     - `onSuccess`: invalidate queries with keys `['communityProfile', followingCmId]`, `['isFollowing']`, `['followerList']`, `['followingList']`
   - Export `useIsFollowing(followerCmId: string, followingCmId: string)`:
     - `useQuery` with `queryKey: ['isFollowing', followerCmId, followingCmId]`
     - `queryFn`: `supabase.from('community_follows').select('id').eq('follower_cm_id', followerCmId).eq('following_cm_id', followingCmId).maybeSingle()`
     - Returns `!!data` (boolean)
     - `enabled: !!followerCmId && !!followingCmId`

2. Create `apps/mobile/hooks/community/useCommunityProfile.ts`:
   - Export `useCommunityProfile(memberId: string)`:
     - `useQuery` with `queryKey: ['communityProfile', memberId]`
     - `queryFn`: `supabase.from('community_members').select('id, user_id, community_id, community_nickname, avatar_url, follower_count, following_count').eq('id', memberId).single()`
     - Return type: `{ id: string; user_id: string; community_id: string; community_nickname: string; avatar_url: string | null; follower_count: number; following_count: number }`
   - Export `useMemberPosts(memberId: string, communityId: string)`:
     - `useQuery` with `queryKey: ['memberPosts', memberId]`
     - `queryFn`: `supabase.from('posts_with_nickname').select('*').eq('community_id', communityId).eq('author_cm_id', memberId).order('created_at', { ascending: false }).limit(50)`
   - Export `useMemberPostCount(memberId: string, communityId: string)`:
     - `useQuery` with `queryKey: ['memberPostCount', memberId]`
     - `queryFn`: `supabase.from('posts').select('*', { count: 'exact', head: true }).eq('community_id', communityId).eq('author_id', ???)` — need user_id from community_members. OR count from posts_with_nickname: `.eq('author_cm_id', memberId)`

3. Create `apps/mobile/hooks/community/useFollowerList.ts`:
   - Export `useFollowerList(memberId: string)`:
     - `useQuery` with `queryKey: ['followerList', memberId]`
     - `queryFn`: query `community_follows` where `following_cm_id = memberId`, join to get follower member data
     - Use: `supabase.from('community_follows').select('id, follower_cm_id, community_members!community_follows_follower_cm_id_fkey(id, community_nickname, avatar_url)').eq('following_cm_id', memberId)`
     - Or simpler: two-step — first get follower_cm_ids, then batch-fetch community_members. Use the PostgREST relationship syntax if it works, otherwise two queries.

4. Create `apps/mobile/hooks/community/useFollowingList.ts`:
   - Same pattern as useFollowerList but query `community_follows` where `follower_cm_id = memberId`, get following members

5. Create `apps/mobile/components/community/FollowButton.tsx`:
   - Props: `followerCmId: string; followingCmId: string; isOwnProfile?: boolean`
   - If `isOwnProfile` return null (no follow button on own profile)
   - Use `useIsFollowing(followerCmId, followingCmId)` and `useFollowMember()`
   - States per UI-SPEC:
     - Not following: `bg-teal text-background rounded-full px-6 py-2` — text "팔로우" from `t('profile.follow')`
     - Following: `bg-card border border-border text-foreground rounded-full px-6 py-2` — text "팔로잉" from `t('profile.following')`
     - Loading: `ActivityIndicator color="#00E5C3"` same size
   - onPress (not following): call `followMutation.mutate({ followerCmId, followingCmId, isFollowing: false })`
   - onPress (following): show `Alert.alert` confirmation dialog (imperative, matching LeaveConfirmDialog/DeleteConfirmDialog pattern):
     - title: `t('profile.unfollowConfirm.title')` — "팔로우 취소"
     - message: `t('profile.unfollowConfirm.body', { nickname })` — need nickname as prop or context
     - buttons: `[{ text: t('profile.unfollowConfirm.dismiss'), style: 'cancel' }, { text: t('profile.unfollowConfirm.confirm'), style: 'destructive', onPress: () => mutate({ ..., isFollowing: true }) }]`
   - Add `nickname?: string` prop for the unfollow confirm dialog body text
   - `accessibilityRole="button"` `accessibilityState={{ selected: isFollowing }}`

6. Create `apps/mobile/app/(community)/[id]/profile/[memberId].tsx`:
   - `useLocalSearchParams<{ id: string; memberId: string }>()`
   - Use `useCommunityProfile(memberId)`, `useMemberPosts(memberId, id)`, `useCommunityMember(id)` (for own cm_id)
   - `useAuthStore()` to check if `profile.user_id === user.id` for own profile detection
   - SafeAreaView bg-background
   - Back button: Pressable `Ionicons chevron-back 24px foreground` touch 44x44, `router.back()`
   - Avatar: 80px circle. If `profile.avatar_url`, render `expo-image` with `contentFit="cover"`. Else `View bg-input rounded-full` with `Ionicons person-outline 32px #666666`.
   - Nickname: `Text text-display font-semibold text-foreground mt-2`
   - Stats row: `View flex-row mt-4 gap-6` with 3 stat columns:
     - Each: `View items-center` with `Text text-heading font-semibold text-foreground` (count) + `Text text-label text-muted-foreground` (label from `t('profile.stat.posts')` etc.)
     - Followers count: Pressable → `router.push(\`/(community)/${id}/profile/${memberId}/followers\` as never)`
     - Following count: Pressable → `router.push(\`/(community)/${id}/profile/${memberId}/following\` as never)`
   - FollowButton: `mt-4`, pass `followerCmId={myCmId}` `followingCmId={memberId}` `isOwnProfile={isOwn}` `nickname={profile.community_nickname}`
   - Tab bar: two tabs (Posts / Comments), active tab has `text-teal` and 2px bottom border `border-teal`
   - Content below tabs:
     - Posts tab: FlashList of PostCard items from `memberPosts` data
     - Comments tab: FlatList of member's comments (query `comments_with_nickname` view or `comments` table filtered by author)
   - Loading skeleton: avatar placeholder 80px circle bg-input, 3 stat placeholders 32x16 bg-input rounded, 3 card placeholders bg-card rounded-xl h-24

7. Create `apps/mobile/app/(community)/[id]/profile/[memberId]/followers.tsx`:
   - `useLocalSearchParams<{ id: string; memberId: string }>()`
   - Use `useFollowerList(memberId)`, `useCommunityMember(id)` for own cm_id
   - SafeAreaView bg-background
   - Header: back button + title "팔로워" (`t('profile.tab.followers')`) text-heading font-semibold
   - FlatList of followers:
     - Each row: `View flex-row items-center px-4 py-2` min height 60px
       - Avatar: 40px circle (expo-image or placeholder View bg-input)
       - Nickname: `Text text-body font-semibold text-foreground ml-4 flex-1`
       - FollowButton (compact) — right-aligned. Pass `followerCmId={myCmId}` `followingCmId={follower.id}`
     - Nickname Pressable: `router.push(\`/(community)/${id}/profile/${follower.id}\` as never)`
   - Empty state: `t('profile.followers.empty')` — "아직 팔로워가 없어요"

8. Create `apps/mobile/app/(community)/[id]/profile/[memberId]/following.tsx`:
   - Same pattern as followers.tsx but uses `useFollowingList(memberId)`
   - Title: "팔로잉" (`t('profile.tab.following')`)
   - Empty state: `t('profile.following.empty')` — "아직 팔로잉하는 멤버가 없어요"

9. Update `apps/mobile/components/post/PostCard.tsx`:
   - Make avatar/nickname area tappable — wrap existing avatar + nickname Views in Pressable
   - onPress: `router.push(\`/(community)/${post.community_id}/profile/${post.author_cm_id}\` as never)`
   - `accessibilityRole="link"` on the pressable
   - Do NOT navigate to profile when tapping own posts (check `post.author_id === user?.id` from authStore)
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/wecord-wv && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/hooks/community/useFollowMember.ts contains "community_follows" and "useMutation" and "follower_cm_id"
    - apps/mobile/hooks/community/useFollowMember.ts exports "useIsFollowing" and "useFollowMember"
    - apps/mobile/hooks/community/useCommunityProfile.ts contains "community_members" and "follower_count" and "following_count"
    - apps/mobile/hooks/community/useFollowerList.ts contains "community_follows" and "following_cm_id"
    - apps/mobile/hooks/community/useFollowingList.ts contains "community_follows" and "follower_cm_id"
    - apps/mobile/components/community/FollowButton.tsx contains "bg-teal" and "profile.follow" and "Alert.alert" and "unfollowConfirm"
    - apps/mobile/components/community/FollowButton.tsx contains "accessibilityState"
    - apps/mobile/app/(community)/[id]/profile/[memberId].tsx contains "useCommunityProfile" and "FollowButton" and "follower_count" and "text-display"
    - apps/mobile/app/(community)/[id]/profile/[memberId].tsx contains "chevron-back" for back navigation
    - apps/mobile/app/(community)/[id]/profile/[memberId]/followers.tsx contains "useFollowerList" and "profile.followers.empty"
    - apps/mobile/app/(community)/[id]/profile/[memberId]/following.tsx contains "useFollowingList" and "profile.following.empty"
    - apps/mobile/components/post/PostCard.tsx contains "author_cm_id" and "profile" for avatar navigation
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Users can follow/unfollow within communities via profile page; follower/following lists are viewable; community profile shows stats and posts/comments tabs; PostCard avatar/nickname taps navigate to profile; all RLS-enforced via existing community_follows policies</done>
</task>

</tasks>

<verification>
- `pnpm --filter mobile test --run` passes
- `npx tsc --noEmit --project apps/mobile/tsconfig.json` passes
- All SRCH and FLLW requirement IDs addressed
- Post search with keyword highlighting works within community context
- Community profile screen renders with follow button and stats
</verification>

<success_criteria>
- SRCH-01: HomeSearchBar from Plan 05-01 navigates to existing community search screen (already done)
- SRCH-02: PostSearchScreen uses full-text search via pg_textsearch on posts_with_nickname
- SRCH-03: HighlightedText renders matched keywords in Teal (#00E5C3)
- FLLW-01: FollowButton calls useFollowMember which inserts/deletes from community_follows
- FLLW-02: FollowerListScreen and FollowingListScreen show member lists with follow buttons
- FLLW-03: CommunityProfileScreen shows nickname, post count, follower count, following count, posts/comments tabs
- FLLW-04: RLS on community_follows table enforces same-community constraint (already implemented in schema)
</success_criteria>

<output>
After completion, create `.planning/phases/05-home-feed-search-community-social/05-02-SUMMARY.md`
</output>
