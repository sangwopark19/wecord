---
phase: 07-launch-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/tests/profile/useUpdateProfile.test.ts
  - apps/mobile/tests/profile/useUploadAvatar.test.ts
  - apps/mobile/tests/profile/dirty-state.test.ts
  - apps/mobile/tests/settings/language-change.test.ts
  - apps/mobile/tests/settings/push-switch.test.ts
  - apps/mobile/tests/community/useMyCommunities.test.ts
  - apps/mobile/tests/auth/signOut.test.ts
  - apps/mobile/vitest.config.ts
  - apps/mobile/package.json
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/shop.tsx
  - apps/mobile/app/(tabs)/dm.tsx
  - apps/mobile/app/(tabs)/more.tsx
  - apps/mobile/app/(more)/_layout.tsx
  - apps/mobile/app/(more)/profile-edit.tsx
  - apps/mobile/app/(more)/settings.tsx
  - apps/mobile/app/(more)/joined-communities.tsx
  - apps/mobile/app/(more)/language.tsx
  - apps/mobile/app/(onboarding)/language.tsx
  - apps/mobile/components/settings/LanguagePicker.tsx
  - apps/mobile/components/settings/SettingsRow.tsx
  - apps/mobile/components/more/ProfileCard.tsx
  - apps/mobile/components/more/JoinedCommunityRow.tsx
  - apps/mobile/components/more/AvatarActionSheet.tsx
  - apps/mobile/hooks/profile/useUpdateProfile.ts
  - apps/mobile/hooks/profile/useUploadAvatar.ts
  - apps/mobile/hooks/profile/useDirtyState.ts
  - apps/mobile/hooks/community/useMyCommunities.ts
  - apps/mobile/stores/authStore.ts
  - packages/db/src/schema/auth.ts
  - packages/shared/src/i18n/locales/ko/more.json
  - packages/shared/src/i18n/locales/en/more.json
  - packages/shared/src/i18n/locales/ko/settings.json
  - packages/shared/src/i18n/locales/en/settings.json
  - packages/shared/src/i18n/locales/ko/common.json
  - packages/shared/src/i18n/locales/en/common.json
  - packages/shared/src/i18n/index.ts
autonomous: true
requirements: [MORE-01, MORE-02, MORE-03, MORE-04, MORE-05]
threat_ids: [T-7-03, T-7-04, T-7-05, T-7-07]
tags: [mobile, expo-router, tabs, profile, i18n, settings, more-tab]

must_haves:
  truths:
    - "User sees 5 tabs at the bottom (Home / Community / Shop / DM / More) with correct Ionicons outline/filled variants"
    - "User tapping More tab sees profile card with avatar+nickname+bio preview, joined communities list, settings entry, app info, logout row"
    - "User can edit nickname (2-20 chars), bio (≤150 chars), and avatar from a single profile-edit screen with a top-right Save button that is disabled until something changes"
    - "User tapping avatar sees ActionSheet with 4 options (camera / library / default avatar / remove-if-custom) and Cancel"
    - "User changing language in Settings sees UI change immediately without app restart and the choice persists to profiles.language"
    - "User sees joined-communities list with community cover + name + my-nickname subtitle; tapping a row navigates to /(community)/[id]"
    - "User toggling the push-notifications global switch reconciles with OS permission state via Notifications.getPermissionsAsync"
    - "User tapping Log Out sees an Alert.alert with Cancel + destructive Log Out button, then is replaced to /(auth)/login with SecureStore token cleared and TanStack Query cache cleared"
  artifacts:
    - path: "apps/mobile/app/(tabs)/_layout.tsx"
      provides: "5-tab bottom navigation with notifications hidden (href:null)"
      contains: "Tabs.Screen name=\"shop\""
    - path: "apps/mobile/app/(tabs)/more.tsx"
      provides: "More tab entry with ProfileCard + joined communities + settings + about + logout sections"
    - path: "apps/mobile/app/(more)/profile-edit.tsx"
      provides: "Single-screen profile editor for nickname+bio+avatar with dirty-state Save button"
    - path: "apps/mobile/app/(more)/settings.tsx"
      provides: "iOS-style grouped Settings page (Language / Push / Per-community / Terms / Privacy / Version)"
    - path: "apps/mobile/app/(more)/joined-communities.tsx"
      provides: "Joined communities list screen backed by useMyCommunities"
    - path: "apps/mobile/components/settings/LanguagePicker.tsx"
      provides: "Shared radio picker extracted from onboarding/language.tsx"
      exports: ["LanguagePicker"]
    - path: "apps/mobile/components/settings/SettingsRow.tsx"
      provides: "Grouped list row primitive (switch / chevron / value / destructive variants)"
      exports: ["SettingsRow"]
    - path: "apps/mobile/components/more/AvatarActionSheet.tsx"
      provides: "Avatar ActionSheet wrapper over @expo/react-native-action-sheet"
      exports: ["useAvatarActionSheet"]
    - path: "apps/mobile/hooks/profile/useUpdateProfile.ts"
      provides: "Optimistic profile mutation for nickname/bio/avatar/language/dm_launch_notify"
      exports: ["useUpdateProfile"]
    - path: "apps/mobile/hooks/profile/useUploadAvatar.ts"
      provides: "Avatar upload hook — expo-image-manipulator resize 512x512 + avatars bucket upsert"
      exports: ["useUploadAvatar", "uploadAvatar"]
    - path: "apps/mobile/hooks/community/useMyCommunities.ts"
      provides: "TanStack Query hook returning joined communities with my nickname"
      exports: ["useMyCommunities"]
    - path: "packages/shared/src/i18n/locales/ko/more.json"
      provides: "Korean copy for More tab and profile edit (per 07-UI-SPEC.md)"
      contains: "프로필 편집"
    - path: "packages/shared/src/i18n/locales/en/more.json"
      provides: "English copy for More tab and profile edit"
      contains: "Edit profile"
    - path: "packages/shared/src/i18n/locales/ko/settings.json"
      provides: "Korean copy for Settings screen"
      contains: "푸시 알림"
    - path: "packages/shared/src/i18n/locales/en/settings.json"
      provides: "English copy for Settings screen"
      contains: "Push notifications"
  key_links:
    - from: "apps/mobile/app/(tabs)/_layout.tsx"
      to: "apps/mobile/app/(tabs)/more.tsx"
      via: "Tabs.Screen name=\"more\""
      pattern: "Tabs\\.Screen\\s+name=\"more\""
    - from: "apps/mobile/app/(tabs)/more.tsx"
      to: "apps/mobile/app/(more)/profile-edit.tsx"
      via: "router.push('/(more)/profile-edit')"
      pattern: "/\\(more\\)/profile-edit"
    - from: "apps/mobile/app/(more)/profile-edit.tsx"
      to: "apps/mobile/hooks/profile/useUpdateProfile.ts + useUploadAvatar.ts"
      via: "hook invocation on Save press"
      pattern: "useUpdateProfile|useUploadAvatar"
    - from: "apps/mobile/app/(more)/language.tsx"
      to: "apps/mobile/components/settings/LanguagePicker.tsx"
      via: "import + mode=\"settings\""
      pattern: "LanguagePicker"
    - from: "apps/mobile/app/(more)/settings.tsx"
      to: "expo-notifications.getPermissionsAsync + Linking.openSettings"
      via: "togglePush reconcile"
      pattern: "getPermissionsAsync|openSettings"
    - from: "apps/mobile/stores/authStore.ts signOut"
      to: "queryClient.clear + router.replace('/(auth)/login')"
      via: "extended signOut"
      pattern: "queryClient\\.clear|router\\.replace"
---

<objective>
Deliver the Phase 7 More tab (account hub), the 5-tab bottom navigation expansion, profile editing, language switching, joined-communities list, settings, and log-out — covering MORE-01..05. This plan also seeds the Wave 0 test stubs for every More-tab test referenced in VALIDATION.md so later waves have a red/green signal to commit against.

Purpose: User gains a single account hub, giving them control over their profile, language, notifications, and session — a launch prerequisite.
Output: Five production-ready screens + four hooks + two shared components + an authStore upgrade, backed by vitest stubs and committed i18n resources.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-launch-polish/07-CONTEXT.md
@.planning/phases/07-launch-polish/07-RESEARCH.md
@.planning/phases/07-launch-polish/07-PATTERNS.md
@.planning/phases/07-launch-polish/07-UI-SPEC.md
@.planning/phases/07-launch-polish/07-VALIDATION.md

<interfaces>
<!-- Extracted from codebase; executor should use these directly. -->

From apps/mobile/stores/authStore.ts (Profile interface — will be extended in Task 2):
```typescript
export interface Profile {
  userId: string;
  globalNickname: string;
  avatarUrl: string | null;
  bio: string | null;          // existing but not always selected — ensure select includes it
  language: SupportedLanguage;
  onboardingCompleted: boolean;
  dateOfBirth: string | null;
  dmLaunchNotify: boolean;     // NEW for Phase 7 (added by this plan)
}
export interface AuthStore {
  session: Session | null;
  user: User | null;
  profile: Profile | null;
  onboardingData: OnboardingData | null;
  setProfile: (profile: Profile | null) => void;
  signOut: () => Promise<void>;  // extended in Task 7 to also call queryClient.clear + router.replace
  // ...
}
```

From apps/mobile/hooks/notification/useNotificationPreferences.ts (analog for optimistic mutation):
```typescript
// Copy onMutate / onError / onSettled cancel+rollback+invalidate pattern verbatim; swap table to 'profiles' and key to ['profile', userId].
```

From apps/mobile/hooks/post/useCreatePost.ts (analog for image upload):
```typescript
// compressAndUploadImage(uri, path) — reuse manipulateAsync + web/native base64 branch + supabase.storage.upload + getPublicUrl two-step.
```

From apps/mobile/app/(onboarding)/language.tsx (LANGUAGES constant — extract as-is):
```typescript
const LANGUAGES: { code: SupportedLanguage; label: string }[] = [
  { code: 'ko', label: '한국어' },
  { code: 'en', label: 'English' },
  { code: 'th', label: 'ภาษาไทย' },
  { code: 'zh', label: '中文' },
  { code: 'ja', label: '日本語' },
];
```

From apps/mobile/app/(community)/[id]/notification-preferences.tsx (PreferenceRow primitive — basis for SettingsRow):
```typescript
// Row: flexDirection:'row', paddingVertical:16, paddingHorizontal:16, borderBottomWidth:1, className:'border-border'
// Switch trackColor={{ false: '#3A3A3A', true: '#00E5C3' }} thumbColor="#FFFFFF"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [Wave 0] Create vitest stubs + mocks + i18n namespaces for More tab</name>
  <read_first>
    - .planning/phases/07-launch-polish/07-VALIDATION.md (Wave 0 Requirements — 7 stub files for MORE-01..05 + shared mocks)
    - .planning/phases/07-launch-polish/07-UI-SPEC.md (Copywriting Contract — More / Settings / Logout copy exact strings)
    - .planning/phases/07-launch-polish/07-PATTERNS.md (Pattern 26 — i18n namespace structure from common.json)
    - apps/mobile/tests/setup.ts (existing vitest setup file)
    - apps/mobile/vitest.config.ts (existing config)
    - packages/shared/src/i18n/locales/ko/common.json (analog flat-key structure)
    - packages/shared/src/i18n/index.ts (resource registration location)
    - apps/mobile/tests/profile.test.ts (existing profile test — may need reference for setup)
  </read_first>
  <behavior>
    - Each test file uses `it.todo('description')` placeholders that vitest reports as SKIPPED (not failing) — matches Phase 4 Wave 0 convention logged in STATE.md ("vitest reports them as skipped, ensuring test run exits 0").
    - `cd apps/mobile && pnpm test` exits 0 with new files visible as skipped.
    - react-native-webview, @expo/react-native-action-sheet, expo-tracking-transparency are mockable in tests (Task 3 of 07-02 will install them; mocks must be resilient to pre-install state by returning stub objects).
    - i18n resources `more` and `settings` load when app initializes (registered in index.ts resources object per namespace).
    - Every copy string in UI-SPEC Copywriting Contract "More tab" and "Settings screen" and "Log Out + Account Deletion" tables appears verbatim in ko/en JSON files.
  </behavior>
  <action>
    **[REVIEW UPDATE — Codex MEDIUM: ATT dependency removal]**
    Do NOT install `expo-tracking-transparency`. Per D-36 the app does not invoke any ATT API and ships without `NSUserTrackingUsageDescription` in Info.plist. Installing-but-not-calling adds package weight for zero behavior — Apple auto-classifies apps without the ATT Info.plist key as "Not tracking" (same outcome as install + no-call). If future Analytics is added, reintroduce via a dedicated mini-plan. This removal is the source-of-truth for Plan 07-03 Task 6 (`app.json` plugins) which also MUST NOT list `expo-tracking-transparency`.

    Install `@expo/react-native-action-sheet` (^4.1.1) via `npx expo install` — run `cd apps/mobile && npx expo install @expo/react-native-action-sheet`. (react-native-webview install is deferred to Plan 07-02 Task 1 since Shop is 07-02 scope.)

    Update `apps/mobile/vitest.config.ts` to add aliases for the new library. Add to the existing `resolve.alias` array (check current config first; if no alias array, create one):
    - `@expo/react-native-action-sheet` → a stub file at `apps/mobile/tests/__mocks__/action-sheet.ts` that exports `useActionSheet` returning `{ showActionSheetWithOptions: vi.fn() }` and `ActionSheetProvider` as a no-op pass-through component.
    - Do NOT alias `expo-tracking-transparency` — the package is not installed and the app does not call any ATT API.

    Create `apps/mobile/tests/__mocks__/action-sheet.ts` with the stub code above. Use ES module syntax. Do NOT create a tracking.ts mock — there is no tracking dependency to mock.

    Create the following Wave 0 test stub files. Each file starts with `import { describe, it } from 'vitest';` and wraps `it.todo()` placeholders in a `describe` block named after the subject. Do NOT write assertions — `it.todo()` only. These are unlocked/filled-in by downstream tasks in this plan (Task 2-7).

    1. `apps/mobile/tests/profile/useUpdateProfile.test.ts`:
       - `describe('useUpdateProfile', () => { it.todo('updates profiles row via supabase.from.update with user-scoped filter'); it.todo('merges returned profile into authStore via setProfile'); it.todo('invalidates [profile, userId] query on settled'); it.todo('rolls back optimistic profile on error (T-7-03: self-only RLS is still the server enforcement, client is optimistic only)'); })`
    2. `apps/mobile/tests/profile/useUploadAvatar.test.ts`:
       - `it.todo('resizes to 512x512 via manipulateAsync with compress 0.8 JPEG')`
       - `it.todo('uploads to avatars bucket at {userId}/avatar-{timestamp}.jpg with upsert:true')`
       - `it.todo('returns public URL from supabase.storage.getPublicUrl')`
       - `it.todo('rejects non-image MIME per UI-SPEC image/jpeg|png|webp allowlist (T-7-04: storage DoS mitigation)')`
       - `it.todo('rejects files over 2MB after compression (T-7-04: size cap)')`
    3. `apps/mobile/tests/profile/dirty-state.test.ts`:
       - `it.todo('returns isDirty=false when current equals initial')`
       - `it.todo('returns isDirty=true when nickname changes')`
       - `it.todo('returns isDirty=true when bio changes')`
       - `it.todo('returns isDirty=true when avatarUrl changes')`
    4. `apps/mobile/tests/settings/language-change.test.ts`:
       - `it.todo('calls i18n.changeLanguage with new code')`
       - `it.todo('updates profiles.language via supabase')`
       - `it.todo('invalidates [profile, userId] query')`
       - `it.todo('persists across app restart via profile.language reload')`
    5. `apps/mobile/tests/settings/push-switch.test.ts`:
       - `it.todo('calls Notifications.requestPermissionsAsync when canAskAgain is true')`
       - `it.todo('calls Linking.openSettings when permission denied and canAskAgain is false')`
       - `it.todo('reconciles local state on screen focus via getPermissionsAsync')`
    6. `apps/mobile/tests/community/useMyCommunities.test.ts`:
       - `it.todo('selects community_members with !inner join to communities filtered by user_id')`
       - `it.todo('orders by joined_at desc')`
       - `it.todo('maps to { communityId, communityName, coverImageUrl, myCommunityNickname, joinedAt } shape')`
       - `it.todo('returns empty array when user has no memberships')`
    7. `apps/mobile/tests/auth/signOut.test.ts`:
       - `it.todo('calls supabase.auth.signOut')`
       - `it.todo('clears session/user/profile/onboardingData in authStore')`
       - `it.todo('invokes queryClient.clear on TanStack cache (T-7-05: stale data eviction)')`
       - `it.todo('replaces router to /(auth)/login stack')`

    Create i18n namespace JSON files. File paths and EXACT content (copy verbatim from 07-UI-SPEC.md Copywriting Contract — no paraphrasing):

    `packages/shared/src/i18n/locales/ko/more.json`:
    ```json
    {
      "tabLabel": "더보기",
      "profileCard": { "editButton": "프로필 편집" },
      "sections": {
        "joinedCommunities": "가입한 커뮤니티",
        "settings": "설정",
        "about": "앱 정보"
      },
      "joinedCommunities": {
        "emptyHeading": "아직 가입한 커뮤니티가 없어요",
        "emptyBody": "홈에서 마음에 드는 크리에이터를 찾아보세요.",
        "myNicknameLabel": "내 닉네임: {{nickname}}"
      },
      "logout": {
        "rowLabel": "로그아웃",
        "dialogTitle": "로그아웃하시겠어요?",
        "dialogCancel": "취소",
        "dialogConfirm": "로그아웃"
      },
      "deleteAccount": { "rowLabel": "계정 삭제" },
      "about": { "versionLabel": "버전" },
      "profileEdit": {
        "screenTitle": "프로필 편집",
        "saveCta": "저장",
        "saveCtaSaving": "저장 중…",
        "nicknameLabel": "닉네임",
        "nicknameHelper": "2~20자 · 중복 허용",
        "nicknameErrorLength": "2자 이상 20자 이하로 입력해주세요.",
        "bioLabel": "소개",
        "bioHelper": "{{count}}/150",
        "avatarActions": {
          "title": "프로필 사진",
          "camera": "카메라로 촬영",
          "library": "앨범에서 선택",
          "useDefault": "기본 아바타로 변경",
          "remove": "사진 삭제",
          "cancel": "취소"
        },
        "toasts": {
          "uploadError": "사진을 업로드하지 못했어요. 다시 시도해주세요.",
          "saveSuccess": "프로필이 저장되었어요.",
          "saveError": "저장하지 못했어요. 잠시 후 다시 시도해주세요."
        }
      }
    }
    ```

    `packages/shared/src/i18n/locales/en/more.json`:
    ```json
    {
      "tabLabel": "More",
      "profileCard": { "editButton": "Edit profile" },
      "sections": {
        "joinedCommunities": "My Communities",
        "settings": "Settings",
        "about": "About"
      },
      "joinedCommunities": {
        "emptyHeading": "No communities yet",
        "emptyBody": "Discover creators on the Home tab.",
        "myNicknameLabel": "My nickname: {{nickname}}"
      },
      "logout": {
        "rowLabel": "Log out",
        "dialogTitle": "Log out?",
        "dialogCancel": "Cancel",
        "dialogConfirm": "Log out"
      },
      "deleteAccount": { "rowLabel": "Delete account" },
      "about": { "versionLabel": "Version" },
      "profileEdit": {
        "screenTitle": "Edit profile",
        "saveCta": "Save",
        "saveCtaSaving": "Saving…",
        "nicknameLabel": "Nickname",
        "nicknameHelper": "2–20 characters · duplicates allowed",
        "nicknameErrorLength": "Enter between 2 and 20 characters.",
        "bioLabel": "Bio",
        "bioHelper": "{{count}}/150",
        "avatarActions": {
          "title": "Profile photo",
          "camera": "Take photo",
          "library": "Choose from library",
          "useDefault": "Use default avatar",
          "remove": "Remove photo",
          "cancel": "Cancel"
        },
        "toasts": {
          "uploadError": "Couldn't upload photo. Try again.",
          "saveSuccess": "Profile saved.",
          "saveError": "Couldn't save. Try again in a moment."
        }
      }
    }
    ```

    `packages/shared/src/i18n/locales/ko/settings.json`:
    ```json
    {
      "screenTitle": "설정",
      "language": { "rowLabel": "언어" },
      "push": {
        "rowLabel": "푸시 알림",
        "helperOff": "시스템 설정에서 알림을 켜주세요.",
        "helperOn": "새로운 댓글과 공지 알림을 받습니다.",
        "openSettingsCta": "설정 열기",
        "openSettingsCancel": "취소"
      },
      "perCommunity": { "rowLabel": "커뮤니티별 알림 설정" },
      "legal": {
        "termsRow": "서비스 이용약관",
        "privacyRow": "개인정보처리방침"
      }
    }
    ```

    `packages/shared/src/i18n/locales/en/settings.json`:
    ```json
    {
      "screenTitle": "Settings",
      "language": { "rowLabel": "Language" },
      "push": {
        "rowLabel": "Push notifications",
        "helperOff": "Turn on notifications in system settings.",
        "helperOn": "Receive new comment and notice alerts.",
        "openSettingsCta": "Open Settings",
        "openSettingsCancel": "Cancel"
      },
      "perCommunity": { "rowLabel": "Per-community notifications" },
      "legal": {
        "termsRow": "Terms of Service",
        "privacyRow": "Privacy Policy"
      }
    }
    ```

    Also add to `packages/shared/src/i18n/locales/ko/common.json` and `en/common.json` the tab labels under a new `tabs` key (merge into existing file — do not overwrite):
    - ko: `"tabs": { "home": "홈", "community": "커뮤니티", "shop": "Shop", "dm": "DM", "more": "더보기" }`
    - en: `"tabs": { "home": "Home", "community": "Community", "shop": "Shop", "dm": "DM", "more": "More" }`

    Register the new namespaces in `packages/shared/src/i18n/index.ts`. Load the existing file, then for each locale (ko/en/th/zh/ja) add to the resources object `more: more_<lang>, settings: settings_<lang>`.

    **[REVIEW UPDATE — Codex MEDIUM: i18n scope]** PROJECT.md §72 mandates 5 UI languages (KO/EN/TH/ZH-CN/JA). Phase 7 MUST ship minimal TH/ZH/JA copy for the `more` and `settings` namespaces — not silent KO fallback, which is unreadable for a Thai/Chinese/Japanese user.

    Create `packages/shared/src/i18n/locales/th/more.json`, `packages/shared/src/i18n/locales/zh/more.json`, `packages/shared/src/i18n/locales/ja/more.json` and the matching `settings.json` trio. Key structure MUST mirror the ko+en files verbatim (same JSON shape). For every string, provide a translation — sources for minimal strings: machine-translate the KO copy as a v1 starter (mark each file with `// generated 2026-04-22; community review needed v1.0.1`). Do NOT ship placeholder KO copy under th/zh/ja filenames.

    Fallback chain update in `packages/shared/src/i18n/index.ts`: set `i18next.init({ fallbackLng: 'en', ... })` — EN is the fallback (not KO), because an English string is more likely to be understood than Korean by a TH/ZH/JA user if any key happens to be missing.

    Import statements follow existing convention (e.g., `import koMore from './locales/ko/more.json';`, plus `import thMore from './locales/th/more.json';` for all 5 language × 2 namespace imports). Total added files this task: 4 ko+en JSONs (existing step) + 6 th+zh+ja JSONs (this REVIEW update) = 10 JSON files in packages/shared/src/i18n/locales/{ko,en,th,zh,ja}/{more,settings}.json.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm test 2>&1 | tail -30</automated>
    <automated>cd apps/mobile && pnpm typecheck 2>&1 | tail -10</automated>
    <automated>grep -r "프로필 편집" packages/shared/src/i18n/locales/ko/more.json</automated>
    <automated>grep -r "Push notifications" packages/shared/src/i18n/locales/en/settings.json</automated>
    <automated>grep -c "it.todo" apps/mobile/tests/profile/useUpdateProfile.test.ts apps/mobile/tests/profile/useUploadAvatar.test.ts apps/mobile/tests/profile/dirty-state.test.ts apps/mobile/tests/settings/language-change.test.ts apps/mobile/tests/settings/push-switch.test.ts apps/mobile/tests/community/useMyCommunities.test.ts apps/mobile/tests/auth/signOut.test.ts</automated>
    <automated>grep "more:" packages/shared/src/i18n/index.ts && grep "settings:" packages/shared/src/i18n/index.ts</automated>
    <!-- REVIEW UPDATE: 5-language resources present for more + settings namespaces -->
    <automated>ls packages/shared/src/i18n/locales/th/more.json packages/shared/src/i18n/locales/zh/more.json packages/shared/src/i18n/locales/ja/more.json packages/shared/src/i18n/locales/th/settings.json packages/shared/src/i18n/locales/zh/settings.json packages/shared/src/i18n/locales/ja/settings.json</automated>
    <automated>grep -q "fallbackLng.*'en'" packages/shared/src/i18n/index.ts || grep -q 'fallbackLng.*"en"' packages/shared/src/i18n/index.ts</automated>
    <!-- REVIEW UPDATE: expo-tracking-transparency must NOT be a dependency -->
    <automated>grep -q "expo-tracking-transparency" apps/mobile/package.json && echo "FAIL: tracking pkg present" || echo "OK: tracking pkg absent"</automated>
    <automated>ls apps/mobile/tests/__mocks__/tracking.ts 2>/dev/null && echo "FAIL: tracking mock present" || echo "OK: tracking mock absent"</automated>
    <automated>cd apps/mobile && pnpm lint 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 7 test stub files exist under apps/mobile/tests/ with it.todo placeholders (counted via grep)
    - vitest reports them as skipped and exit code is 0 (`pnpm test` succeeds)
    - `@expo/react-native-action-sheet` installed in `apps/mobile/package.json` dependencies (tracking package NOT installed per review update)
    - Vitest mock for action-sheet exists at `apps/mobile/tests/__mocks__/action-sheet.ts` (no tracking.ts mock)
    - 10 i18n JSON files exist: 4 ko/en × more/settings (verbatim UI-SPEC copy) + 6 th/zh/ja × more/settings (machine-translated starter, marked for community review)
    - `tabs` key added to both common.json files
    - Namespaces registered in packages/shared/src/i18n/index.ts resources
    - `fallbackLng: 'en'` set in i18next config (not KO)
    - Typecheck passes
    - **Manual note (INFO 2):** Update `.planning/phases/07-launch-polish/07-VALIDATION.md` frontmatter `wave_0_complete` to `true` once 07-02 Task 1 (shop/dm/account Wave 0 stubs) is also complete. Until both Wave 0 tasks finish, keep `wave_0_complete: false`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema extension + hooks (useUpdateProfile / useUploadAvatar / useDirtyState / useMyCommunities)</name>
  <read_first>
    - packages/db/src/schema/auth.ts (current profiles table definition lines 13-28)
    - apps/mobile/stores/authStore.ts (Profile interface + fetchOrCreateProfile select statement)
    - apps/mobile/hooks/notification/useNotificationPreferences.ts (optimistic mutation analog — onMutate/onError/onSettled exact pattern, lines 43-90)
    - apps/mobile/hooks/post/useCreatePost.ts (compressAndUploadImage pattern, lines 17-48 — bucket + base64 + web/native branch)
    - apps/mobile/hooks/community/useCommunitySearch.ts (useQuery + mapping analog, lines 15-34)
    - .planning/phases/07-launch-polish/07-RESEARCH.md (Pattern 5 dirty-state, Pattern 6 avatar upload, Example 1 useMyCommunities)
    - .planning/phases/07-launch-polish/07-PATTERNS.md (sections 10, 11, 12 — exact analogs)
    - apps/mobile/tests/profile/useUpdateProfile.test.ts (stub file — fill out it.todo into real tests)
    - apps/mobile/tests/profile/useUploadAvatar.test.ts
    - apps/mobile/tests/profile/dirty-state.test.ts
    - apps/mobile/tests/community/useMyCommunities.test.ts
  </read_first>
  <behavior>
    - `useUpdateProfile()` returns a mutation that accepts `Partial<{ globalNickname, bio, avatarUrl, language, dmLaunchNotify }>`; on mutate updates optimistically via authStore.setProfile; on success keeps update; on error rolls back; on settled invalidates `['profile', userId]`.
    - `useUploadAvatar()` returns `{ mutateAsync(localUri: string): Promise<string> }` that resizes to 512x512 JPEG at quality 0.8, uploads to `avatars/{userId}/avatar-{Date.now()}.jpg` with `upsert: true`, returns the public URL. Rejects localUri that leads to >2MB output or non-image MIME (T-7-04).
    - `useDirtyState<T>(initial: T)` utility returns `{ current, setCurrent, isDirty, reset() }` where isDirty is true if shallow-compare finds any field differs. Pure utility — no React Query dependency.
    - `useMyCommunities()` returns useQuery for joined communities sorted by joined_at desc, shape: `{ communityId, communityName, communitySlug, coverImageUrl, myCommunityNickname, joinedAt }[]`.
    - Profile interface extended with `bio: string | null` (re-confirm — may already exist) and `dmLaunchNotify: boolean`. authStore.fetchOrCreateProfile SELECT includes `bio, dm_launch_notify`.
    - T-7-03 (Profile update spoofing) mitigated by existing `profiles_update_own` RLS — update hook uses `.eq('user_id', user.id)` but server enforces self-only regardless.
  </behavior>
  <action>
    **Step 1 — Extend profiles schema in Drizzle (packages/db/src/schema/auth.ts):**
    Add the column `dmLaunchNotify: boolean('dm_launch_notify').notNull().default(false)` to the `profiles` pgTable definition (follow existing column order; place after `settings` or before `updatedAt`). Re-export unchanged.

    **Step 2 — Extend authStore Profile interface (apps/mobile/stores/authStore.ts):**
    1. Add `dmLaunchNotify: boolean` to the `Profile` interface (per D-28).
    2. In `fetchOrCreateProfile`, add `bio, dm_launch_notify` to the `.select(...)` string so these fields populate. When mapping the returned row, include `bio: row.bio ?? null, dmLaunchNotify: row.dm_launch_notify ?? false`.
    3. No changes to signOut yet — that is Task 7.

    **Step 3 — Create `apps/mobile/hooks/profile/useUpdateProfile.ts`:**
    Mirror `useNotificationPreferences.ts` mutation structure. Key signature:
    ```typescript
    export interface UpdateProfileVars {
      globalNickname?: string;
      bio?: string | null;
      avatarUrl?: string | null;
      language?: SupportedLanguage;
      dmLaunchNotify?: boolean;
    }
    export function useUpdateProfile() { ... }
    ```
    Mutation body:
    - Guard `const { user, profile, setProfile } = useAuthStore(); if (!user || !profile) throw new Error('unauthenticated');`
    - Build supabase column patch: `{ global_nickname?, bio?, avatar_url?, language?, dm_launch_notify? }` from vars (snake_case mapping).
    - `const { error } = await supabase.from('profiles').update(patch).eq('user_id', user.id);`
    - If error, throw.
    - onMutate: cancel `['profile', user.id]`, snapshot previous profile, optimistically setProfile({ ...profile, ...camelVars }), return { previous }.
    - onError: setProfile(context.previous).
    - onSettled: queryClient.invalidateQueries({ queryKey: ['profile', user.id] }).

    **Step 4 — Create `apps/mobile/hooks/profile/useUploadAvatar.ts`:**
    Copy `compressAndUploadImage` structure from useCreatePost.ts verbatim but change:
    - Resize: `[{ resize: { width: 512, height: 512 } }]` and `{ compress: 0.8, format: SaveFormat.JPEG }`.
    - Bucket: `'avatars'` (not `'post-media'`).
    - Path: `${userId}/avatar-${Date.now()}.jpg`.
    - `upsert: true` (new avatar overwrites).
    - Before upload, check compressed file size (use `expo-file-system` `getInfoAsync` on `compressed.uri` — import at top `import * as FileSystem from 'expo-file-system';`). If `.size > 2097152` throw Error('Avatar too large after compression').
    Export as both a standalone async function `uploadAvatar(userId, uri): Promise<string>` and a TanStack-wrapped hook `useUploadAvatar()` returning `{ mutateAsync, isPending, error }` so tests can exercise either.

    **Step 5 — Create `apps/mobile/hooks/profile/useDirtyState.ts`:**
    Pure React hook, no external deps:
    ```typescript
    import { useState, useRef } from 'react';
    export function useDirtyState<T extends Record<string, unknown>>(initial: T) {
      const initialRef = useRef(initial);
      const [current, setCurrent] = useState<T>(initial);
      const isDirty = (Object.keys(initialRef.current) as (keyof T)[])
        .some((k) => !Object.is(current[k], initialRef.current[k]));
      const reset = () => { initialRef.current = current; };
      return { current, setCurrent, isDirty, reset };
    }
    ```

    **Step 6 — Create `apps/mobile/hooks/community/useMyCommunities.ts`:**
    Copy exactly from RESEARCH Example 1 (lines 921-961). Interface:
    ```typescript
    export interface JoinedCommunity {
      communityId: string;
      communityName: string;
      communitySlug: string;
      coverImageUrl: string | null;
      myCommunityNickname: string;
      joinedAt: string;
    }
    ```
    Query:
    - `useAuthStore((s) => s.user?.id)` → userId
    - `['myCommunities', userId]` queryKey
    - `enabled: !!userId`
    - `.from('community_members').select('community_nickname, joined_at, communities!inner(id, name, slug, cover_image_url)').eq('user_id', userId!).order('joined_at', { ascending: false })`
    - Map to `JoinedCommunity[]` shape.

    **Step 7 — Replace it.todo placeholders in the four corresponding test files with real vitest tests** using `vi.mock('../../lib/supabase')` patterns already established in apps/mobile/tests/profile.test.ts. For useUploadAvatar: mock `expo-image-manipulator.manipulateAsync` returning a fake URI, mock `expo-file-system.getInfoAsync` returning `{ size: 500_000 }` for happy path and `{ size: 3_000_000 }` for oversize rejection, mock `supabase.storage.from('avatars').upload` + `getPublicUrl`.

    Do NOT touch the remaining three stubs (language-change, push-switch, signOut) — those are finalized in Tasks 4, 5, 7.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm test tests/profile/useUpdateProfile.test.ts tests/profile/useUploadAvatar.test.ts tests/profile/dirty-state.test.ts tests/community/useMyCommunities.test.ts 2>&1 | tail -20</automated>
    <automated>cd apps/mobile && pnpm typecheck 2>&1 | tail -10</automated>
    <automated>grep -n "dm_launch_notify" packages/db/src/schema/auth.ts</automated>
    <automated>grep -n "dmLaunchNotify" apps/mobile/stores/authStore.ts</automated>
    <automated>grep -n "avatars" apps/mobile/hooks/profile/useUploadAvatar.ts</automated>
    <automated>grep -n "community_members" apps/mobile/hooks/community/useMyCommunities.ts</automated>
    <automated>cd apps/mobile && pnpm lint 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Four hook files created (useUpdateProfile, useUploadAvatar, useDirtyState, useMyCommunities) with exports matching signatures above
    - Profile interface includes dmLaunchNotify: boolean; Drizzle schema has dm_launch_notify column
    - fetchOrCreateProfile SELECT includes bio and dm_launch_notify
    - Four test files pass with real tests (no more it.todo for these four)
    - Typecheck passes
    - T-7-03 mitigation documented in useUpdateProfile action (.eq user_id filter)
    - T-7-04 mitigation: MIME + size check in useUploadAvatar
  </done>
</task>

<task type="auto">
  <name>Task 2b: [REVIEW HIGH — Route ordering] Create minimal placeholder shop.tsx + dm.tsx BEFORE 5-tab wiring</name>
  <read_first>
    - apps/mobile/app/(tabs)/_layout.tsx (current Tabs.Screen list — 2 visible + notifications hidden)
    - apps/mobile/app/(tabs)/index.tsx (existing tab screen file structure)
    - .planning/phases/07-launch-polish/07-REVIEWS.md (Codex HIGH — Route ordering concern)
  </read_first>
  <behavior>
    - `apps/mobile/app/(tabs)/shop.tsx` and `apps/mobile/app/(tabs)/dm.tsx` exist as minimal placeholder route files BEFORE Task 3 adds Tabs.Screen entries for `shop` and `dm`.
    - This prevents the Expo Router "route declared but file missing" warning/error when 07-01 is executed in isolation (before 07-02 ships the full Shop/DM screens).
    - The placeholders render only a centered SafeAreaView + "Coming soon" label in the current language — NO hooks, NO WebView, NO mutations. They are wholly thrown away and overwritten by Plan 07-02 Task 3 (shop) and Task 4 (dm) which replace them with production content.
    - Placeholders use existing i18n keys (`common:tabs.shop`, `common:tabs.dm`) so nothing new has to be added to common.json beyond the keys already in Task 1.
  </behavior>
  <action>
    **Why this task exists (REVIEW reference):** Codex review (07-REVIEWS.md, Plan 07-01 HIGH — "Route ordering") flagged that Task 3 declares `<Tabs.Screen name="shop" />` and `<Tabs.Screen name="dm" />` while the actual route files live in Plan 07-02. Expo Router v7 can throw a hard error ("Screen with name 'shop' does not match any route") when a Screen name does not resolve. This task creates zero-content route files so the wiring in Task 3 resolves cleanly.

    **Step 1 — Create `apps/mobile/app/(tabs)/shop.tsx` (placeholder only):**
    ```tsx
    import { SafeAreaView } from 'react-native-safe-area-context';
    import { View, Text } from 'react-native';
    import { useTranslation } from '@wecord/shared/i18n';

    // PHASE 7 PLACEHOLDER — overwritten by Plan 07-02 Task 3 (ShopWebView wiring).
    // Exists so that Plan 07-01 Task 3's Tabs.Screen name="shop" resolves cleanly
    // when 07-01 is executed in isolation. Do not add hooks/state here — minimal
    // render only.
    export default function ShopPlaceholderScreen() {
      const { t } = useTranslation();
      return (
        <SafeAreaView className="flex-1 bg-background items-center justify-center">
          <Text className="text-heading font-semibold text-foreground">{t('common:tabs.shop')}</Text>
          <Text className="text-body text-muted-foreground mt-2">Coming soon</Text>
        </SafeAreaView>
      );
    }
    ```

    **Step 2 — Create `apps/mobile/app/(tabs)/dm.tsx` (placeholder only):**
    ```tsx
    import { SafeAreaView } from 'react-native-safe-area-context';
    import { View, Text } from 'react-native';
    import { useTranslation } from '@wecord/shared/i18n';

    // PHASE 7 PLACEHOLDER — overwritten by Plan 07-02 Task 4 (DmPlaceholder wiring).
    export default function DmPlaceholderScreen() {
      const { t } = useTranslation();
      return (
        <SafeAreaView className="flex-1 bg-background items-center justify-center">
          <Text className="text-heading font-semibold text-foreground">{t('common:tabs.dm')}</Text>
          <Text className="text-body text-muted-foreground mt-2">Coming soon</Text>
        </SafeAreaView>
      );
    }
    ```

    **Step 3 — Overwrite warning in SUMMARY:** In 07-01-SUMMARY.md, record that both files are intentional placeholders owned by 07-02 Task 3 + Task 4. Plan 07-02 executors MUST overwrite these files, not merge on top.
  </action>
  <verify>
    <automated>ls apps/mobile/app/\(tabs\)/shop.tsx apps/mobile/app/\(tabs\)/dm.tsx</automated>
    <automated>grep -q "PHASE 7 PLACEHOLDER" apps/mobile/app/\(tabs\)/shop.tsx</automated>
    <automated>grep -q "PHASE 7 PLACEHOLDER" apps/mobile/app/\(tabs\)/dm.tsx</automated>
    <automated>grep -q "Coming soon" apps/mobile/app/\(tabs\)/shop.tsx apps/mobile/app/\(tabs\)/dm.tsx</automated>
    <automated>cd apps/mobile && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `apps/mobile/app/(tabs)/shop.tsx` exists as placeholder with "PHASE 7 PLACEHOLDER" comment and no hooks
    - `apps/mobile/app/(tabs)/dm.tsx` exists as placeholder with "PHASE 7 PLACEHOLDER" comment and no hooks
    - Typecheck clean
    - Plan 07-02 Task 3/4 overwrite these files — record in 07-01-SUMMARY.md
    - Task 3 (5-tab wiring) can declare `name="shop"` and `name="dm"` without Expo Router resolution errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 5-tab expansion + More hub screen + shared components (LanguagePicker / SettingsRow / ProfileCard / JoinedCommunityRow / AvatarActionSheet)</name>
  <read_first>
    - apps/mobile/app/(tabs)/_layout.tsx (current 2-tab + hidden notifications config)
    - apps/mobile/app/(tabs)/notifications.tsx (SafeAreaView + header + ScrollView pattern, lines 170-247)
    - apps/mobile/app/(onboarding)/language.tsx (LanguagePicker extraction source, full file lines 13-88)
    - apps/mobile/app/(community)/[id]/notification-preferences.tsx (PreferenceRow primitive + header pattern, lines 9-41 and 58-79)
    - apps/mobile/app/_layout.tsx (to wrap in ActionSheetProvider)
    - apps/mobile/components/community/CommunityCard.tsx (card layout analog for ProfileCard)
    - apps/mobile/components/notification/NotificationRow.tsx (row with chevron analog for JoinedCommunityRow)
    - .planning/phases/07-launch-polish/07-UI-SPEC.md (5-tab bottom navigation table, More tab profile card, Joined communities list, Settings/About/Logout sections)
    - .planning/phases/07-launch-polish/07-PATTERNS.md (sections 1, 2, 15, 16, 17, 18, 19)
  </read_first>
  <behavior>
    - `(tabs)/_layout.tsx` renders exactly 5 visible Tabs.Screen (index, community, shop, dm, more) + 1 hidden (notifications with `href:null`), in that order. Each visible Screen has a `tabBarIcon` that switches between outline (inactive) and filled (focused) Ionicons: home/home-outline, people/people-outline, bag/bag-outline, chatbubbles/chatbubbles-outline, person/person-outline. Labels come from `t('common:tabs.home')` etc.
    - `shop` and `dm` screens may not exist yet (they're created in 07-02). Tabs.Screen entries for `shop` and `dm` are declared anyway; if the route files don't exist they will show a blank screen — acceptable for 07-01 since both plans ship in the same phase before release.
    - `(tabs)/more.tsx` shows: ProfileCard → "가입한 커뮤니티" section (first 5 joined + "모두 보기" chevron row → /(more)/joined-communities) → "설정" row (chevron → /(more)/settings) → "앱 정보" section (Terms / Privacy / Version rows) → "로그아웃" destructive row.
    - `AvatarActionSheet` is usable via a hook `useAvatarActionSheet({ hasCustomAvatar, onCamera, onLibrary, onUseDefault, onRemove })` that returns `{ show(): void }`.
    - `ActionSheetProvider` wraps the app in `apps/mobile/app/_layout.tsx` so all downstream screens can use action sheets.
    - `LanguagePicker` exports a component with props `{ value, onChange, mode: 'onboarding' | 'settings' }`. Onboarding mode behaves as before (parent commits on CTA press). Settings mode invokes onChange immediately and the parent screen commits.
  </behavior>
  <action>
    **Step 1 — Wrap root layout with ActionSheetProvider (apps/mobile/app/_layout.tsx):**
    Import `{ ActionSheetProvider } from '@expo/react-native-action-sheet'`. Wrap the current Stack / providers tree so ActionSheetProvider is outside QueryClientProvider (it needs to be at the top-level component tree). Follow the same conditional Web/Native guard already in the file if present.

    **Step 2 — Update `apps/mobile/app/(tabs)/_layout.tsx`:**
    Preserve the existing `<Tabs screenOptions={{ headerShown: false, tabBarStyle: { backgroundColor: '#000000', borderTopColor: '#2A2A2A', height: 56 }, tabBarActiveTintColor: '#00E5C3', tabBarInactiveTintColor: '#999999' }}>` block.

    Replace the inner Tabs.Screen list with exactly this (in this order):
    ```tsx
    <Tabs.Screen name="index" options={{ title: t('common:tabs.home'), tabBarIcon: ({ focused, color, size }) => <Ionicons name={focused ? 'home' : 'home-outline'} size={size} color={color} /> }} />
    <Tabs.Screen name="community" options={{ title: t('common:tabs.community'), tabBarIcon: ({ focused, color, size }) => <Ionicons name={focused ? 'people' : 'people-outline'} size={size} color={color} /> }} />
    <Tabs.Screen name="shop" options={{ title: t('common:tabs.shop'), tabBarIcon: ({ focused, color, size }) => <Ionicons name={focused ? 'bag' : 'bag-outline'} size={size} color={color} /> }} />
    <Tabs.Screen name="dm" options={{ title: t('common:tabs.dm'), tabBarIcon: ({ focused, color, size }) => <Ionicons name={focused ? 'chatbubbles' : 'chatbubbles-outline'} size={size} color={color} /> }} />
    <Tabs.Screen name="more" options={{ title: t('common:tabs.more'), tabBarIcon: ({ focused, color, size }) => <Ionicons name={focused ? 'person' : 'person-outline'} size={size} color={color} /> }} />
    <Tabs.Screen name="notifications" options={{ href: null }} />
    ```
    Import `useTranslation` from `@wecord/shared/i18n` and call at function top: `const { t } = useTranslation();`. Import `Ionicons` from `@expo/vector-icons`.

    **Step 3 — Create shared components:**

    a) `apps/mobile/components/settings/LanguagePicker.tsx`: Copy LANGUAGES constant from onboarding/language.tsx verbatim (lines 13-19). Export component:
    ```tsx
    interface Props {
      value: SupportedLanguage;
      onChange: (lang: SupportedLanguage) => void;
      mode: 'onboarding' | 'settings';
    }
    export function LanguagePicker({ value, onChange, mode }: Props) {
      return (
        <FlatList
          data={LANGUAGES}
          keyExtractor={(item) => item.code}
          className="mt-4"
          ItemSeparatorComponent={() => <View className="h-2" />}
          renderItem={({ item }) => {
            const isSelected = item.code === value;
            return (
              <Pressable
                accessibilityRole="radio"
                accessibilityState={{ selected: isSelected }}
                accessibilityLabel={item.label}
                onPress={() => onChange(item.code)}
                className={`flex-row items-center justify-between h-[52px] rounded-xl px-4 bg-card ${isSelected ? 'border-2 border-teal' : 'border-2 border-transparent'}`}
              >
                <Text className="text-body font-regular text-foreground">{item.label}</Text>
                <View className={`w-5 h-5 rounded-full border-2 items-center justify-center ${isSelected ? 'border-teal' : 'border-subtle'}`}>
                  {isSelected && <View className="w-[10px] h-[10px] rounded-full bg-teal" />}
                </View>
              </Pressable>
            );
          }}
        />
      );
    }
    ```
    The `mode` prop is currently only documented for future expansion — behavior differs at the call site (settings call `onChange` triggers immediate commit; onboarding collects and commits on CTA). No branching inside the component itself.

    b) Refactor `apps/mobile/app/(onboarding)/language.tsx` to use the new LanguagePicker:
    - Delete local LANGUAGES array and the inline FlatList rendering.
    - Import `{ LanguagePicker } from '../../components/settings/LanguagePicker'`.
    - Replace the FlatList JSX with `<LanguagePicker value={selectedLanguage} onChange={setSelectedLanguage} mode="onboarding" />`.
    - Keep existing `handleContinue` unchanged (onboarding still commits via CTA).

    c) `apps/mobile/components/settings/SettingsRow.tsx`:
    ```tsx
    interface Props {
      leftIcon?: React.ComponentProps<typeof Ionicons>['name'];
      label: string;
      value?: string;              // shows right-of-label when right='value' or 'valueWithChevron'
      right?: 'switch' | 'chevron' | 'valueWithChevron' | 'none';
      switchValue?: boolean;
      onSwitchChange?: (v: boolean) => void;
      destructive?: boolean;       // red text
      onPress?: () => void;
      isFirst?: boolean;           // rounded-t-12
      isLast?: boolean;            // rounded-b-12
    }
    ```
    - Row height 48px.
    - `bg-card`.
    - `borderBottomWidth: 1, borderBottomColor: '#2A2A2A'` (only if `!isLast`).
    - Left: 20px outline Ionicon if `leftIcon` provided, 8px gap to label.
    - Label: `text-body font-regular`. If `destructive`, `text-destructive`.
    - Right content by variant:
      - `switch`: `<Switch value={switchValue} onValueChange={onSwitchChange} trackColor={{ false: '#2A2A2A', true: '#00E5C3' }} thumbColor="#FFFFFF" accessibilityRole="switch" />`
      - `chevron`: `<Ionicons name="chevron-forward" size={16} color="#999999" />`
      - `valueWithChevron`: `<Text className="text-body text-muted-foreground mr-1">{value}</Text><Ionicons name="chevron-forward" size={16} color="#999999" />`
      - `value`: `<Text className="text-body text-muted-foreground">{value}</Text>`
      - `none`: nothing
    - Pressable wrapper with `accessibilityRole="button"` and `accessibilityLabel={label}` when `onPress` is provided.

    d) `apps/mobile/components/more/ProfileCard.tsx`:
    - Container: `<View className="bg-card rounded-xl mx-4 p-4 flex-row items-center" style={{ height: 88 }}>`.
    - Avatar: `<Image source={profile.avatarUrl ? { uri: profile.avatarUrl } : undefined}>` using `expo-image`, size 56x56, `rounded-full`. If no avatarUrl, render initial-letter View (first char of globalNickname) over teal bg (match existing CommunityCard fallback pattern).
    - Middle flex: globalNickname (`text-heading font-semibold text-foreground`), bio preview (`text-body text-muted-foreground`, `numberOfLines={1}`).
    - Right: `<Pressable onPress={() => router.push('/(more)/profile-edit')} accessibilityRole="button" accessibilityLabel={t('more:profileCard.editButton')}><Text className="text-label text-teal">{t('more:profileCard.editButton')}</Text></Pressable>`.

    e) `apps/mobile/components/more/JoinedCommunityRow.tsx`:
    - `<Pressable>` 56px height, `borderBottomWidth: 1, borderBottomColor: '#2A2A2A'`, `flex-row items-center px-4`.
    - Left: 40dp circular `expo-image` cover (fallback: teal View with first letter).
    - Middle flex: name (`text-body font-semibold text-foreground`) + subtitle `t('more:joinedCommunities.myNicknameLabel', { nickname })` (`text-label text-muted-foreground`).
    - Right: `<Ionicons name="chevron-forward" size={16} color="#999999" />`.
    - onPress: `router.push(\`/(community)/\${communityId}\`)`.

    f) `apps/mobile/components/more/AvatarActionSheet.tsx`:
    ```tsx
    import { useActionSheet } from '@expo/react-native-action-sheet';
    import { useTranslation } from '@wecord/shared/i18n';
    interface Options {
      hasCustomAvatar: boolean;
      onCamera: () => void;
      onLibrary: () => void;
      onUseDefault: () => void;
      onRemove?: () => void;
    }
    export function useAvatarActionSheet({ hasCustomAvatar, onCamera, onLibrary, onUseDefault, onRemove }: Options) {
      const { showActionSheetWithOptions } = useActionSheet();
      const { t } = useTranslation();
      return {
        show: () => {
          const options = [
            t('more:profileEdit.avatarActions.camera'),
            t('more:profileEdit.avatarActions.library'),
            t('more:profileEdit.avatarActions.useDefault'),
            ...(hasCustomAvatar ? [t('more:profileEdit.avatarActions.remove')] : []),
            t('more:profileEdit.avatarActions.cancel'),
          ];
          const destructiveButtonIndex = hasCustomAvatar ? 3 : undefined;
          const cancelButtonIndex = options.length - 1;
          showActionSheetWithOptions(
            { options, destructiveButtonIndex, cancelButtonIndex, title: t('more:profileEdit.avatarActions.title') },
            (selectedIndex) => {
              if (selectedIndex === 0) onCamera();
              else if (selectedIndex === 1) onLibrary();
              else if (selectedIndex === 2) onUseDefault();
              else if (hasCustomAvatar && selectedIndex === 3 && onRemove) onRemove();
            }
          );
        },
      };
    }
    ```

    **Step 4 — Create `apps/mobile/app/(more)/_layout.tsx`:**
    Copy `apps/mobile/app/(community)/[id]/_layout.tsx` verbatim (Stack with `screenOptions={{ headerShown: false, contentStyle: { backgroundColor: '#000000' } }}`), rename default export to `MoreGroupLayout`.

    **Step 5 — Create `apps/mobile/app/(tabs)/more.tsx`:**
    Structure:
    ```tsx
    <SafeAreaView className="flex-1 bg-background">
      <View className="border-b border-border px-4 py-3">
        <Text className="text-display font-semibold text-foreground">{t('more:tabLabel')}</Text>
      </View>
      <ScrollView className="flex-1">
        <View className="mt-4"><ProfileCard profile={profile} /></View>

        <Text className="text-label text-muted-foreground pl-4 pt-6 pb-2">{t('more:sections.joinedCommunities')}</Text>
        {isLoading ? <ActivityIndicator className="my-4" color="#00E5C3" /> :
          joined.length === 0 ? (
            <View className="bg-card rounded-xl mx-4 p-8 items-center">
              <Text className="text-heading font-semibold text-foreground text-center">{t('more:joinedCommunities.emptyHeading')}</Text>
              <Text className="text-body text-muted-foreground text-center mt-2">{t('more:joinedCommunities.emptyBody')}</Text>
            </View>
          ) : (
            <View className="bg-card rounded-xl mx-4 overflow-hidden">
              {joined.slice(0, 5).map((c, i) => <JoinedCommunityRow key={c.communityId} community={c} isLast={i === Math.min(joined.length, 5) - 1 && joined.length <= 5} />)}
              {joined.length > 5 && <Pressable onPress={() => router.push('/(more)/joined-communities')} className="h-[48px] flex-row items-center justify-between px-4">
                <Text className="text-body text-foreground">모두 보기</Text>
                <Ionicons name="chevron-forward" size={16} color="#999999" />
              </Pressable>}
            </View>
          )
        }

        <Text className="text-label text-muted-foreground pl-4 pt-6 pb-2">{t('more:sections.settings')}</Text>
        <View className="bg-card rounded-xl mx-4 overflow-hidden">
          <SettingsRow leftIcon="settings-outline" label={t('settings:screenTitle')} right="chevron" isFirst isLast onPress={() => router.push('/(more)/settings')} />
        </View>

        <Text className="text-label text-muted-foreground pl-4 pt-6 pb-2">{t('more:sections.about')}</Text>
        <View className="bg-card rounded-xl mx-4 overflow-hidden">
          <SettingsRow label={t('settings:legal.termsRow')} right="chevron" isFirst onPress={() => Linking.openURL(TERMS_URL)} />
          <SettingsRow label={t('settings:legal.privacyRow')} right="chevron" onPress={() => Linking.openURL(PRIVACY_URL)} />
          <SettingsRow label={t('more:about.versionLabel')} right="value" value={Application.nativeApplicationVersion ?? ''} isLast />
        </View>

        <View className="mt-6 mx-4 bg-card rounded-xl overflow-hidden mb-12">
          <SettingsRow label={t('more:logout.rowLabel')} destructive right="none" isFirst isLast onPress={handleLogoutPress} />
        </View>
      </ScrollView>
    </SafeAreaView>
    ```
    - `TERMS_URL` and `PRIVACY_URL` — declare as const at the top of the file. Use placeholder `https://wecord-docs.pages.dev/terms?lang=ko` and `https://wecord-docs.pages.dev/privacy?lang=ko` (actual domain set in Plan 07-03; these constants are updated there). Leave a `// TODO(07-03): replace with production admin URL` comment.
    - `handleLogoutPress` implementation is placeholder in this task (temporarily shows Alert.alert with title/cancel/confirm; onPress of confirm calls `useAuthStore.getState().signOut().then(() => router.replace('/(auth)/login'))`). Task 7 upgrades signOut itself to do cache.clear + router.replace internally; until then the call site handles the replace.
    - Import `* as Application from 'expo-application'` — if not installed, use `require('../../package.json').version`; check presence first with `ls apps/mobile/node_modules/expo-application` before choosing.
    - Log out confirmation implemented inline: `Alert.alert(t('more:logout.dialogTitle'), '', [{ text: t('more:logout.dialogCancel'), style: 'cancel' }, { text: t('more:logout.dialogConfirm'), style: 'destructive', onPress: async () => { await useAuthStore.getState().signOut(); router.replace('/(auth)/login'); } }]);`.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm typecheck 2>&1 | tail -10</automated>
    <automated>grep -c "Tabs.Screen" apps/mobile/app/\(tabs\)/_layout.tsx</automated>
    <automated>grep -E "name=\"shop\"|name=\"dm\"|name=\"more\"" apps/mobile/app/\(tabs\)/_layout.tsx</automated>
    <automated>grep -n "href: null" apps/mobile/app/\(tabs\)/_layout.tsx</automated>
    <automated>grep "LanguagePicker" apps/mobile/app/\(onboarding\)/language.tsx</automated>
    <automated>grep "ActionSheetProvider" apps/mobile/app/_layout.tsx</automated>
    <automated>grep "chatbubbles-outline" apps/mobile/app/\(tabs\)/_layout.tsx</automated>
    <automated>grep -n "bag-outline\|bag" apps/mobile/app/\(tabs\)/_layout.tsx</automated>
    <automated>grep "프로필 편집" apps/mobile/components/more/ProfileCard.tsx || grep "editButton" apps/mobile/components/more/ProfileCard.tsx</automated>
    <automated>cd apps/mobile && pnpm test 2>&1 | tail -20</automated>
    <automated>cd apps/mobile && pnpm lint 2>&1 | tail -10</automated>
    <!-- INFO 1: D-02/D-03/D-04 invariants — 5 visible Tabs.Screen + notifications hidden via href:null; community route renders its Fan/Artist/Highlight sub-tabs -->
    <automated>test $(grep -c "Tabs.Screen" apps/mobile/app/\(tabs\)/_layout.tsx) -eq 6</automated>
    <automated>grep -q "href: null" apps/mobile/app/\(tabs\)/_layout.tsx</automated>
    <automated>test -f apps/mobile/app/\(community\)/\[id\]/index.tsx && grep -q "Fan\\|Artist\\|Highlight" apps/mobile/app/\(community\)/\[id\]/index.tsx</automated>
  </verify>
  <done>
    - (tabs)/_layout.tsx has exactly 5 visible Tabs.Screen (index, community, shop, dm, more) + notifications with href:null
    - Each icon uses focused-based outline/filled variant
    - ActionSheetProvider wraps app/_layout.tsx
    - onboarding/language.tsx uses LanguagePicker component (no local FlatList)
    - ProfileCard, JoinedCommunityRow, AvatarActionSheet, SettingsRow, LanguagePicker all exist under components/{more,settings}
    - (tabs)/more.tsx renders ProfileCard + joined list + settings entry + about section + logout row
    - (more)/_layout.tsx is a Stack group layout
    - Typecheck passes; existing tests still pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Profile edit screen + joined-communities screen + language settings screen</name>
  <read_first>
    - apps/mobile/app/(onboarding)/language.tsx (state + update pattern, lines 21-49)
    - apps/mobile/app/(community)/[id]/notification-preferences.tsx (header pattern lines 58-79)
    - apps/mobile/app/(community)/search.tsx (FlatList + empty state pattern, lines 30-50)
    - apps/mobile/hooks/profile/useUpdateProfile.ts (created in Task 2)
    - apps/mobile/hooks/profile/useUploadAvatar.ts (created in Task 2)
    - apps/mobile/hooks/profile/useDirtyState.ts (created in Task 2)
    - apps/mobile/hooks/community/useMyCommunities.ts (created in Task 2)
    - apps/mobile/components/settings/LanguagePicker.tsx (created in Task 3)
    - apps/mobile/components/more/ProfileCard.tsx (created in Task 3)
    - apps/mobile/components/more/JoinedCommunityRow.tsx (created in Task 3)
    - apps/mobile/components/more/AvatarActionSheet.tsx (created in Task 3)
    - apps/mobile/components/PrimaryCTAButton.tsx
    - .planning/phases/07-launch-polish/07-UI-SPEC.md (Profile edit screen section, Joined communities list)
    - .planning/phases/07-launch-polish/07-PATTERNS.md (sections 6, 8, 15)
    - apps/mobile/tests/settings/language-change.test.ts (fill into real tests in this task)
  </read_first>
  <behavior>
    - `/(more)/profile-edit` shows: native back arrow + centered title "프로필 편집" + right "저장" button. Save disabled until `isDirty && !loading`. Avatar at top (96dp, tap to open ActionSheet). Nickname input (2-20 chars). Bio textarea (≤150, counter).
    - Save press: uploads avatar if local URI, then `useUpdateProfile` mutate with patched fields; on success toast "프로필이 저장되었어요." and router.back(); on error toast saveError.
    - Nickname validation: trimmed length must be 2-20; show `t('more:profileEdit.nicknameErrorLength')` inline helper when invalid.
    - `/(more)/joined-communities` shows FlatList of JoinedCommunityRow items from useMyCommunities; loading → ActivityIndicator; empty → emptyHeading + emptyBody from more.json.
    - `/(more)/language` uses LanguagePicker mode="settings" — onChange fires `i18n.changeLanguage(code)` immediately AND `useUpdateProfile({ language: code })`; then `router.back()`. Effect: UI re-renders in new language without restart; profile.language persists to DB.
    - Language setting after app restart: user sees same language (because authStore.initialize reads profile.language and calls i18n.changeLanguage — existing behavior, no new wiring needed but document in verify).
  </behavior>
  <action>
    **Step 1 — Create `apps/mobile/app/(more)/profile-edit.tsx`:**
    Structure:
    ```tsx
    export default function ProfileEditScreen() {
      const router = useRouter();
      const { t } = useTranslation();
      const profile = useAuthStore((s) => s.profile);
      if (!profile) { router.back(); return null; }
      const { current, setCurrent, isDirty, reset } = useDirtyState({
        globalNickname: profile.globalNickname,
        bio: profile.bio ?? '',
        avatarUrl: profile.avatarUrl,
        localAvatarUri: null as string | null,
      });
      const updateProfile = useUpdateProfile();
      const uploadAvatar = useUploadAvatar();
      const { show: showAvatarSheet } = useAvatarActionSheet({
        hasCustomAvatar: current.avatarUrl != null,
        onCamera: async () => { const res = await ImagePicker.launchCameraAsync({ allowsEditing: true, aspect: [1, 1], quality: 0.8 }); if (!res.canceled) setCurrent({ ...current, localAvatarUri: res.assets[0].uri }); },
        onLibrary: async () => { const res = await ImagePicker.launchImageLibraryAsync({ mediaTypes: ImagePicker.MediaTypeOptions.Images, allowsEditing: true, aspect: [1, 1], quality: 0.8 }); if (!res.canceled) setCurrent({ ...current, localAvatarUri: res.assets[0].uri }); },
        onUseDefault: () => setCurrent({ ...current, avatarUrl: null, localAvatarUri: null }),
        onRemove: () => setCurrent({ ...current, avatarUrl: null, localAvatarUri: null }),
      });

      const nicknameTrimmed = current.globalNickname.trim();
      const nicknameValid = nicknameTrimmed.length >= 2 && nicknameTrimmed.length <= 20;
      const canSave = isDirty && nicknameValid && !updateProfile.isPending && !uploadAvatar.isPending;

      const handleSave = async () => {
        try {
          let avatarUrl = current.avatarUrl;
          if (current.localAvatarUri) {
            avatarUrl = await uploadAvatar.mutateAsync(current.localAvatarUri);
          }
          await updateProfile.mutateAsync({
            globalNickname: nicknameTrimmed,
            bio: current.bio.trim() || null,
            avatarUrl,
          });
          reset();
          Alert.alert(t('more:profileEdit.toasts.saveSuccess'));
          router.back();
        } catch (err) {
          Alert.alert(t('more:profileEdit.toasts.saveError'));
        }
      };

      return (
        <KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : 'height'} className="flex-1 bg-background">
          <SafeAreaView className="flex-1">
            <View className="border-b border-border px-4 py-3 flex-row items-center justify-between">
              <Pressable onPress={() => router.back()} accessibilityRole="button" accessibilityLabel={t('more:profileEdit.screenTitle')}>
                <Ionicons name="arrow-back" size={24} color="#FFFFFF" />
              </Pressable>
              <Text className="text-heading font-semibold text-foreground">{t('more:profileEdit.screenTitle')}</Text>
              <Pressable onPress={handleSave} disabled={!canSave} accessibilityRole="button" accessibilityLabel={t('more:profileEdit.saveCta')}>
                <Text className={`text-heading font-semibold ${canSave ? 'text-teal' : 'text-subtle'}`}>
                  {(updateProfile.isPending || uploadAvatar.isPending) ? t('more:profileEdit.saveCtaSaving') : t('more:profileEdit.saveCta')}
                </Text>
              </Pressable>
            </View>
            <ScrollView className="flex-1 px-4">
              <View className="items-center mt-6">
                <Pressable onPress={showAvatarSheet} accessibilityRole="button" accessibilityLabel={t('more:profileEdit.avatarActions.title')}>
                  <Image
                    source={current.localAvatarUri ? { uri: current.localAvatarUri } : current.avatarUrl ? { uri: current.avatarUrl } : undefined}
                    style={{ width: 96, height: 96, borderRadius: 48, backgroundColor: '#1A1A1A' }}
                  />
                </Pressable>
              </View>
              <View style={{ marginTop: 24 }}>
                <Text className="text-label text-muted-foreground mb-2">{t('more:profileEdit.nicknameLabel')}</Text>
                <TextInput
                  value={current.globalNickname}
                  onChangeText={(v) => setCurrent({ ...current, globalNickname: v })}
                  maxLength={20}
                  className="bg-card rounded-xl px-4 text-body text-foreground"
                  style={{ height: 48 }}
                  accessibilityLabel={t('more:profileEdit.nicknameLabel')}
                />
                <Text className={`text-label mt-1 ${nicknameValid ? 'text-muted-foreground' : 'text-destructive'}`}>
                  {nicknameValid ? t('more:profileEdit.nicknameHelper') : t('more:profileEdit.nicknameErrorLength')}
                </Text>
              </View>
              <View style={{ marginTop: 24 }}>
                <Text className="text-label text-muted-foreground mb-2">{t('more:profileEdit.bioLabel')}</Text>
                <TextInput
                  value={current.bio}
                  onChangeText={(v) => setCurrent({ ...current, bio: v })}
                  maxLength={150}
                  multiline
                  className="bg-card rounded-xl px-4 py-3 text-body text-foreground"
                  style={{ minHeight: 96, textAlignVertical: 'top' }}
                  accessibilityLabel={t('more:profileEdit.bioLabel')}
                />
                <Text className="text-label text-muted-foreground mt-1 text-right">
                  {t('more:profileEdit.bioHelper', { count: current.bio.length })}
                </Text>
              </View>
            </ScrollView>
          </SafeAreaView>
        </KeyboardAvoidingView>
      );
    }
    ```

    **Step 2 — Create `apps/mobile/app/(more)/joined-communities.tsx`:**
    ```tsx
    export default function JoinedCommunitiesScreen() {
      const router = useRouter();
      const { t } = useTranslation();
      const { data, isLoading } = useMyCommunities();
      return (
        <SafeAreaView className="flex-1 bg-background">
          <View className="border-b border-border px-4 py-3 flex-row items-center">
            <Pressable onPress={() => router.back()} accessibilityRole="button" accessibilityLabel="back" style={{ marginRight: 12 }}>
              <Ionicons name="arrow-back" size={24} color="#FFFFFF" />
            </Pressable>
            <Text className="text-heading font-semibold text-foreground">{t('more:sections.joinedCommunities')}</Text>
          </View>
          {isLoading ? <ActivityIndicator className="mt-12" color="#00E5C3" /> :
           (data ?? []).length === 0 ? (
            <View className="flex-1 items-center justify-center px-8">
              <Ionicons name="people-outline" size={48} color="#666666" />
              <Text className="text-heading font-semibold text-foreground mt-4 text-center">{t('more:joinedCommunities.emptyHeading')}</Text>
              <Text className="text-body text-muted-foreground mt-2 text-center">{t('more:joinedCommunities.emptyBody')}</Text>
            </View>
           ) : (
            <FlatList
              data={data}
              keyExtractor={(item) => item.communityId}
              renderItem={({ item }) => <JoinedCommunityRow community={item} />}
            />
           )}
        </SafeAreaView>
      );
    }
    ```

    **Step 3 — Create `apps/mobile/app/(more)/language.tsx`:**
    ```tsx
    export default function LanguageSettingsScreen() {
      const router = useRouter();
      const { t, i18n } = useTranslation();
      const profile = useAuthStore((s) => s.profile);
      const updateProfile = useUpdateProfile();
      if (!profile) { router.back(); return null; }
      const handleChange = async (code: SupportedLanguage) => {
        await i18n.changeLanguage(code);
        await updateProfile.mutateAsync({ language: code });
        router.back();
      };
      return (
        <SafeAreaView className="flex-1 bg-background">
          <View className="border-b border-border px-4 py-3 flex-row items-center">
            <Pressable onPress={() => router.back()} accessibilityRole="button" accessibilityLabel="back" style={{ marginRight: 12 }}>
              <Ionicons name="arrow-back" size={24} color="#FFFFFF" />
            </Pressable>
            <Text className="text-heading font-semibold text-foreground">{t('settings:language.rowLabel')}</Text>
          </View>
          <View className="px-4">
            <LanguagePicker value={profile.language} onChange={handleChange} mode="settings" />
          </View>
        </SafeAreaView>
      );
    }
    ```

    **Step 4 — Replace it.todo with real tests in `apps/mobile/tests/settings/language-change.test.ts`:**
    Mock `@wecord/shared/i18n` (especially i18n.changeLanguage) and `useUpdateProfile`. Test cases:
    - Assert `i18n.changeLanguage` is called with the new code.
    - Assert `useUpdateProfile.mutateAsync` is called with `{ language: <newCode> }`.
    - Assert router.back is called after both succeed.
    Use existing test patterns from `apps/mobile/tests/profile.test.ts` as setup reference.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm typecheck 2>&1 | tail -10</automated>
    <automated>cd apps/mobile && pnpm test tests/settings/language-change.test.ts 2>&1 | tail -15</automated>
    <automated>grep -n "useDirtyState\|useUpdateProfile\|useUploadAvatar" apps/mobile/app/\(more\)/profile-edit.tsx</automated>
    <automated>grep -n "useMyCommunities" apps/mobile/app/\(more\)/joined-communities.tsx</automated>
    <automated>grep -n "i18n.changeLanguage" apps/mobile/app/\(more\)/language.tsx</automated>
    <automated>grep -n "maxLength={150}\|maxLength={20}" apps/mobile/app/\(more\)/profile-edit.tsx</automated>
    <automated>grep -n "저장\|Save" packages/shared/src/i18n/locales/ko/more.json packages/shared/src/i18n/locales/en/more.json</automated>
    <automated>cd apps/mobile && pnpm lint 2>&1 | tail -10</automated>
  </verify>
  <done>
    - profile-edit.tsx has single-screen form with Save button in header, disabled when !isDirty
    - Avatar tap triggers ActionSheet with 4 options (+Cancel when custom exists)
    - Nickname maxLength 20; bio maxLength 150 with counter
    - joined-communities.tsx renders list via useMyCommunities with empty and loading states
    - /(more)/language.tsx commits language change via i18n.changeLanguage + profile update
    - language-change test passes
    - Typecheck passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Settings screen (language / push / per-community / legal / version)</name>
  <read_first>
    - apps/mobile/app/(community)/[id]/notification-preferences.tsx (header + Switch pattern full file)
    - apps/mobile/components/settings/SettingsRow.tsx (created in Task 3)
    - apps/mobile/hooks/profile/useUpdateProfile.ts (created in Task 2)
    - .planning/phases/07-launch-polish/07-UI-SPEC.md (Settings screen section, Copywriting Contract)
    - .planning/phases/07-launch-polish/07-RESEARCH.md (Pattern 7 push permission reconcile — lines 534-556)
    - apps/mobile/tests/settings/push-switch.test.ts (fill into real tests)
    - packages/shared/src/i18n/locales/ko/settings.json
    - packages/shared/src/i18n/locales/en/settings.json
    - apps/mobile/hooks/useAuth.ts (profile access pattern)
  </read_first>
  <behavior>
    - `/(more)/settings` renders grouped iOS-style list in this order:
      Section "설정": Language row (valueWithChevron showing current language label → /(more)/language), Push row (switch), Per-community row (chevron → /(more)/joined-communities which will re-use the existing screen's navigation; pressing a community row there routes to (community)/[id]/notification-preferences — existing Phase 4 screen).
      Section "앱 정보" (also present in More tab but repeated here for in-settings access is redundant; per D-08 keep only in Settings single page is the spec — BUT D-09 says App Info is a separate section. Per CONTEXT D-08, Settings is the single page that lists language/notifications/terms/privacy/version; per D-09, App Info is a separate section within More tab. Keep Settings to contain: Language + Push + Per-community + Terms + Privacy + Version. The More tab's About section is a shortcut to Terms/Privacy/Version only, with the primary Settings entry also leading here.)
    - Push switch behavior:
      - Initial state reconciled on focus: call `Notifications.getPermissionsAsync()`; set local state = (status === 'granted').
      - OFF → ON press: if canAskAgain → requestPermissionsAsync; if denied & !canAskAgain → Alert with openSettings CTA via Linking.openSettings().
      - **[REVIEW UPDATE — Codex LOW: Push OFF UX]** ON → OFF press: the app CANNOT revoke OS notification permission — only the user can, via system Settings. Correct pattern:
        1. Show confirm Alert: "알림을 끄려면 시스템 설정에서 꺼야 합니다." (same copy already in `settings:push.helperOff`).
        2. Alert buttons: Cancel + "설정 열기" (opens system via `Linking.openSettings()`).
        3. While user is in system Settings, keep local `pushEnabled` state UNCHANGED (do NOT optimistically flip to false).
        4. When user returns to the app, `useFocusEffect` re-fires `Notifications.getPermissionsAsync()`; the switch then reflects the true OS state (granted → switch ON, denied → switch OFF).
        5. If user cancels the Alert, do nothing — switch stays ON because permission is still granted.
      - This guarantees the switch state always mirrors OS truth (never diverges).
    - Terms/Privacy rows open the admin public URL via Linking.openURL with `?lang={currentLang}` query. URLs are the same placeholders used in More tab (Task 3); updated in Plan 07-03.
    - Version row shows `Application.nativeApplicationVersion` (read-only, no onPress).
  </behavior>
  <action>
    **Step 1 — Create `apps/mobile/app/(more)/settings.tsx`:**
    ```tsx
    import { useFocusEffect } from 'expo-router';
    import * as Notifications from 'expo-notifications';
    import * as Linking from 'expo-linking';
    import * as Application from 'expo-application'; // fallback to package.json version if not installed

    const LANG_LABEL: Record<SupportedLanguage, string> = {
      ko: '한국어', en: 'English', th: 'ภาษาไทย', zh: '中文', ja: '日本語',
    };

    export default function SettingsScreen() {
      const router = useRouter();
      const { t, i18n } = useTranslation();
      const profile = useAuthStore((s) => s.profile);
      const [pushEnabled, setPushEnabled] = useState(false);
      const [canAskAgain, setCanAskAgain] = useState(true);

      useFocusEffect(useCallback(() => {
        (async () => {
          const { status, canAskAgain: ask } = await Notifications.getPermissionsAsync();
          setPushEnabled(status === 'granted');
          setCanAskAgain(ask);
        })();
      }, []));

      const togglePush = async (desired: boolean) => {
        if (desired) {
          const { status, canAskAgain: ask } = await Notifications.getPermissionsAsync();
          if (status === 'granted') { setPushEnabled(true); return; }
          if (ask) {
            const { status: newStatus } = await Notifications.requestPermissionsAsync();
            setPushEnabled(newStatus === 'granted');
          } else {
            Alert.alert(t('settings:push.rowLabel'), t('settings:push.helperOff'), [
              { text: t('settings:push.openSettingsCancel'), style: 'cancel' },
              { text: t('settings:push.openSettingsCta'), onPress: () => Linking.openSettings() },
            ]);
          }
        } else {
          Alert.alert(t('settings:push.rowLabel'), t('settings:push.helperOff'), [
            { text: t('settings:push.openSettingsCancel'), style: 'cancel' },
            { text: t('settings:push.openSettingsCta'), onPress: () => Linking.openSettings() },
          ]);
        }
      };

      const termsUrl = `https://wecord-docs.pages.dev/terms?lang=${i18n.language}`;
      const privacyUrl = `https://wecord-docs.pages.dev/privacy?lang=${i18n.language}`;
      const version = Application.nativeApplicationVersion ?? '1.0.0';

      return (
        <SafeAreaView className="flex-1 bg-background">
          <View className="border-b border-border px-4 py-3 flex-row items-center">
            <Pressable onPress={() => router.back()} accessibilityRole="button" accessibilityLabel="back" style={{ marginRight: 12 }}>
              <Ionicons name="arrow-back" size={24} color="#FFFFFF" />
            </Pressable>
            <Text className="text-heading font-semibold text-foreground">{t('settings:screenTitle')}</Text>
          </View>
          <ScrollView>
            <View className="bg-card rounded-xl mx-4 mt-6 overflow-hidden">
              <SettingsRow leftIcon="language-outline" label={t('settings:language.rowLabel')} right="valueWithChevron" value={LANG_LABEL[profile!.language]} isFirst onPress={() => router.push('/(more)/language')} />
              <SettingsRow leftIcon="notifications-outline" label={t('settings:push.rowLabel')} right="switch" switchValue={pushEnabled} onSwitchChange={togglePush} />
              <SettingsRow leftIcon="people-outline" label={t('settings:perCommunity.rowLabel')} right="chevron" isLast onPress={() => router.push('/(more)/joined-communities')} />
            </View>
            <Text className="text-label text-muted-foreground pl-4 mt-2">{pushEnabled ? t('settings:push.helperOn') : t('settings:push.helperOff')}</Text>

            <View className="bg-card rounded-xl mx-4 mt-6 overflow-hidden">
              <SettingsRow leftIcon="document-text-outline" label={t('settings:legal.termsRow')} right="chevron" isFirst onPress={() => Linking.openURL(termsUrl)} />
              <SettingsRow leftIcon="shield-checkmark-outline" label={t('settings:legal.privacyRow')} right="chevron" onPress={() => Linking.openURL(privacyUrl)} />
              <SettingsRow leftIcon="information-circle-outline" label={t('more:about.versionLabel')} right="value" value={version} isLast />
            </View>
          </ScrollView>
        </SafeAreaView>
      );
    }
    ```
    - If `expo-application` is not installed, replace `Application.nativeApplicationVersion` with `require('../../app.json').expo.version` (check before writing — run `ls apps/mobile/node_modules/expo-application 2>/dev/null || true` and adjust).

    **Step 2 — Replace it.todo with real tests in `apps/mobile/tests/settings/push-switch.test.ts`:**
    Mock `expo-notifications` (`getPermissionsAsync`, `requestPermissionsAsync`) and `expo-linking` (`openSettings`). Test cases:
    - `togglePush(true)` when status='undetermined' and canAskAgain=true → calls `requestPermissionsAsync`, sets pushEnabled based on result.
    - `togglePush(true)` when status='denied' and canAskAgain=false → shows Alert with Open Settings → onPress calls `Linking.openSettings()`.
    - `togglePush(false)` always shows Alert (cannot disable from app).
    - Initial focus: `getPermissionsAsync` is called and state is reconciled.
    Use React Testing Library `renderHook` or a tiny function test that isolates `togglePush` logic (extract as a helper if needed to facilitate testing: `export async function reconcilePushToggle(desired: boolean, apis): Promise<boolean>`).
    If an extracted helper makes testing simpler, add it to a shared location (`apps/mobile/hooks/settings/usePushPermission.ts`) and adjust settings.tsx to call it.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm typecheck 2>&1 | tail -10</automated>
    <automated>cd apps/mobile && pnpm test tests/settings/push-switch.test.ts 2>&1 | tail -15</automated>
    <automated>grep -n "Notifications.getPermissionsAsync\|Notifications.requestPermissionsAsync" apps/mobile/app/\(more\)/settings.tsx</automated>
    <automated>grep -n "Linking.openSettings" apps/mobile/app/\(more\)/settings.tsx</automated>
    <automated>grep -n "termsUrl\|privacyUrl\|pages.dev" apps/mobile/app/\(more\)/settings.tsx</automated>
    <automated>grep -n "SettingsRow" apps/mobile/app/\(more\)/settings.tsx | wc -l</automated>
    <automated>cd apps/mobile && pnpm lint 2>&1 | tail -10</automated>
  </verify>
  <done>
    - settings.tsx renders 6 SettingsRow instances (language, push, per-community, terms, privacy, version)
    - Push switch reconciles with OS via getPermissionsAsync on focus (returning from system settings re-reads OS state and updates switch)
    - OFF→ON flows through requestPermissionsAsync; permanent-deny flows through Linking.openSettings
    - **[REVIEW UPDATE — Codex LOW]** ON→OFF press does NOT optimistically flip the local switch; it shows confirm dialog → opens OS settings → reconciles via useFocusEffect on return. Verified by test case "ON→OFF shows Alert and keeps switch ON until OS state changes".
    - Terms/Privacy rows open Linking.openURL with ?lang= param
    - push-switch test passes with ≥4 assertions covering request, deny-then-settings, focus-reconcile on return, and ON→OFF no-optimistic-flip paths
    - Typecheck passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Extend authStore.signOut to clear TanStack cache and replace route (MORE-05 + T-7-05 mitigation)</name>
  <read_first>
    - apps/mobile/stores/authStore.ts (existing signOut implementation, lines 158-161)
    - apps/mobile/lib/queryClient.ts (or wherever QueryClient is configured — check imports in app/_layout.tsx)
    - apps/mobile/app/(tabs)/more.tsx (handleLogoutPress placeholder to simplify after extension)
    - apps/mobile/tests/auth/signOut.test.ts (fill into real tests)
    - .planning/phases/07-launch-polish/07-UI-SPEC.md (Logout confirmation section)
    - .planning/phases/07-launch-polish/07-RESEARCH.md (Pitfall 5 — session ordering)
  </read_first>
  <behavior>
    - `authStore.signOut()` now performs, in order: (1) `await supabase.auth.signOut()`, (2) clear authStore state (session/user/profile/onboardingData to null), (3) invoke `queryClient.clear()` to drop all cached data (T-7-05 mitigation — no stale data after logout).
    - `signOut` does NOT call `router.replace` itself (Zustand store must remain framework-agnostic); the caller handles navigation.
    - SecureStore token removal is handled transparently by `supabase.auth.signOut()` because the adapter persists tokens on sign-in and is instructed by supabase-js to clear them on sign-out — verify by reading ExpoSecureStoreAdapter implementation (already in authStore).
    - More tab `handleLogoutPress` unchanged: Alert confirm → `await useAuthStore.getState().signOut(); router.replace('/(auth)/login')`. Cache clear now happens inside signOut.
  </behavior>
  <action>
    **[REVIEW UPDATE — Codex MEDIUM: Dependency cycle risk]**
    Direct `queryClient` import from inside `authStore.ts` creates a Zustand ↔ TanStack cycle risk: `authStore` depends on `queryClient`, but any hook that depends on BOTH (read user from auth + fetch with query client) pulls them into the same import graph. Instead, keep `authStore` framework-agnostic by using an **injected cleanup callback** registered at app root.

    **Step 1 — Locate queryClient export:**
    Run `grep -rn "new QueryClient" apps/mobile/` to find where QueryClient is instantiated. It is typically exported from `apps/mobile/lib/queryClient.ts` or created inside `apps/mobile/app/_layout.tsx`. If it is inlined in _layout.tsx, extract it to `apps/mobile/lib/queryClient.ts`:
    ```ts
    import { QueryClient } from '@tanstack/react-query';
    export const queryClient = new QueryClient({ /* existing defaults */ });
    ```
    Then import `queryClient` in _layout.tsx instead of `new QueryClient()`.

    **Step 2 — Modify `apps/mobile/stores/authStore.ts` (NO queryClient import):**
    Do NOT add `import { queryClient } from '../lib/queryClient';`. Instead, extend the store's internal shape with a registered `onSignOut` callback slot, and call it from `signOut()`:
    ```ts
    // In AuthStore interface:
    type OnSignOutCallback = () => void | Promise<void>;
    interface AuthStore {
      // ...existing fields...
      onSignOut: OnSignOutCallback | null;
      registerOnSignOut: (cb: OnSignOutCallback) => () => void; // returns unregister
      signOut: () => Promise<void>;
    }

    // In create():
    onSignOut: null,
    registerOnSignOut: (cb) => {
      set({ onSignOut: cb });
      return () => set({ onSignOut: null });
    },
    signOut: async () => {
      try {
        await supabase.auth.signOut();
      } finally {
        const cb = get().onSignOut;
        set({ session: null, user: null, profile: null, onboardingData: null });
        if (cb) { try { await cb(); } catch (e) { /* non-blocking — logout MUST complete */ } }
      }
    },
    ```
    The `try/finally` guarantees state clear even if supabase.auth.signOut rejects. The callback is nullable; if no consumer registered, signOut is still safe.

    **Step 3 — Register the cleanup at app root (`apps/mobile/app/_layout.tsx`):**
    Inside the root layout component (after queryClient + authStore are both available), register the callback with a one-time effect:
    ```tsx
    import { useEffect } from 'react';
    import { queryClient } from '../lib/queryClient';
    import { useAuthStore } from '../stores/authStore';

    // ...inside RootLayout component, after QueryClientProvider wraps:
    const registerOnSignOut = useAuthStore((s) => s.registerOnSignOut);
    useEffect(() => {
      const unregister = registerOnSignOut(() => {
        queryClient.clear();
      });
      return unregister;
    }, [registerOnSignOut]);
    ```
    This keeps `authStore` import graph clean (no TanStack import) — satisfies Codex MEDIUM "dependency cycle risk".

    **Step 4 — Replace it.todo with real tests in `apps/mobile/tests/auth/signOut.test.ts`:**
    Mock `../../lib/supabase` (`supabase.auth.signOut`). DO NOT mock queryClient inside authStore tests — authStore no longer imports it. Test cases:
    1. Calls `supabase.auth.signOut`.
    2. Sets session/user/profile/onboardingData to null.
    3. When `registerOnSignOut(cb)` has been called, `cb` is invoked during signOut after state clear.
    4. When no callback registered, signOut still completes (cb===null path).
    5. Even when `supabase.auth.signOut` throws, the finally block still clears state and invokes registered cb (T-7-05).
    6. When the registered cb throws, signOut does NOT re-throw (logout MUST complete regardless — assert console-swallow only).

    Separately, add `apps/mobile/tests/auth/signOut-queryclient-integration.test.ts` with a test that mounts a minimal component registering the real `queryClient.clear` callback via `registerOnSignOut`, primes the cache with a fake query, calls `signOut`, and asserts the cache is empty afterward. This is the T-7-05 end-to-end proof that the split architecture still clears cached data.

    **Step 5 — Simplify `more.tsx` log-out handler (cosmetic):**
    No code change required — current handler `useAuthStore.getState().signOut().then(() => router.replace('/(auth)/login'))` now benefits from the upgraded signOut. Add comment: `// T-7-05: signOut invokes the root-registered onSignOut callback (queryClient.clear); caller only owns navigation replacement`.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm typecheck 2>&1 | tail -10</automated>
    <automated>cd apps/mobile && pnpm test tests/auth/signOut.test.ts tests/auth/signOut-queryclient-integration.test.ts 2>&1 | tail -20</automated>
    <!-- REVIEW UPDATE: authStore MUST NOT import queryClient — injected callback pattern -->
    <automated>grep -q "from '../lib/queryClient'" apps/mobile/stores/authStore.ts && echo "FAIL: authStore still imports queryClient directly" || echo "OK: no direct queryClient import"</automated>
    <automated>grep -n "registerOnSignOut\|onSignOut" apps/mobile/stores/authStore.ts</automated>
    <automated>grep -n "registerOnSignOut" apps/mobile/app/_layout.tsx</automated>
    <automated>grep -n "try {.*finally\|finally {" apps/mobile/stores/authStore.ts</automated>
    <automated>cd apps/mobile && pnpm lint 2>&1 | tail -10</automated>
  </verify>
  <done>
    - authStore.signOut wraps supabase.auth.signOut in try/finally and invokes the registered onSignOut callback (queryClient.clear) without importing queryClient directly
    - `registerOnSignOut` slot exists on authStore; `_layout.tsx` registers `queryClient.clear` via the slot in a useEffect
    - signOut test has ≥6 assertions covering T-7-05 finally-block guarantee + registered/unregistered paths + cb throwing path
    - Integration test confirms queryClient.clear is invoked end-to-end (primed cache emptied after signOut)
    - Typecheck passes
    - More tab logout continues to work (existing Alert → signOut → replace flow)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Integration verification + Apple Sign-In prominence snapshot extension (T-7-07 preparation)</name>
  <read_first>
    - apps/mobile/app/(auth)/login.tsx (Apple + Google button ordering + sizes)
    - apps/mobile/tests/auth/login-snapshot.test.tsx (create if missing; extend existing if present)
    - .planning/phases/07-launch-polish/07-UI-SPEC.md (accessibility baseline, copy contract)
    - .planning/phases/07-launch-polish/07-RESEARCH.md (Apple Guideline 4.8 checklist, Pitfall 4)
    - apps/mobile/app/(tabs)/_layout.tsx (verify 5 tabs)
    - apps/mobile/app/(tabs)/more.tsx (verify ProfileCard + sections render)
  </read_first>
  <behavior>
    - `tests/auth/login-snapshot.test.tsx` locks: Apple Sign-In button is present AND rendered ABOVE OR AT THE SAME POSITION AS Google Sign-In (measured via child index in the rendered container). Button width/height props match between the two.
    - T-7-07: failing this test prevents shipping; Plan 07-03 submission task re-reads the snapshot.
    - Full vitest suite passes end-of-plan.
    - Typecheck passes.
    - Lint passes for all changed files (per user's global CLAUDE.md — never bypass hooks).
  </behavior>
  <action>
    **Step 1 — Create or extend `apps/mobile/tests/auth/login-snapshot.test.tsx`:**
    Prerequisite: ensure `apps/mobile/app/(auth)/login.tsx` exposes `testID="apple-signin-button"` on the Apple button wrapper and `testID="google-signin-button"` on the Google button wrapper. This is a 1-line addition per button and is the cleanest way to locate the two buttons without walking the fiber tree. Make that change first; then implement the test below. This eliminates the need for any `findIndex` helper and removes all `any` types from the test file.

    ```tsx
    import React from 'react';
    import { describe, it, expect, vi } from 'vitest';
    import { render } from '@testing-library/react-native';
    import type { ReactTestInstance } from 'react-test-renderer';
    import LoginScreen from '../../app/(auth)/login';

    // Typed mocks — no `any` anywhere
    vi.mock('expo-router', () => ({
      useRouter: () => ({ push: vi.fn(), replace: vi.fn() }),
      Link: ({ children }: { children: React.ReactNode }) => children,
    }));
    vi.mock('../../lib/supabase', () => ({
      supabase: { auth: { signInWithIdToken: vi.fn(), signInWithOAuth: vi.fn() } },
    }));
    vi.mock('expo-apple-authentication', () => {
      const AppleAuthenticationButton: React.FC<React.PropsWithChildren<{ testID?: string }>> = (props) =>
        React.createElement('View', { testID: props.testID ?? 'apple-signin-button' }, props.children);
      AppleAuthenticationButton.displayName = 'AppleAuthenticationButton';
      return {
        signInAsync: vi.fn(),
        AppleAuthenticationButton,
        AppleAuthenticationButtonType: { SIGN_IN: 0 },
        AppleAuthenticationButtonStyle: { WHITE: 0, BLACK: 1, WHITE_OUTLINE: 2 },
        AppleAuthenticationScope: { FULL_NAME: 0, EMAIL: 1 },
      };
    });

    describe('Login screen — T-7-07 Apple Sign-In prominence', () => {
      it('renders Apple sign-in button', () => {
        const { getByTestId } = render(<LoginScreen />);
        expect(getByTestId('apple-signin-button')).toBeTruthy();
      });

      it('renders Google sign-in button', () => {
        const { getByTestId } = render(<LoginScreen />);
        expect(getByTestId('google-signin-button')).toBeTruthy();
      });

      it('Apple button appears at or above Google button in layout order (Guideline 4.8)', () => {
        const { getByTestId, UNSAFE_root } = render(<LoginScreen />);
        const appleNode = getByTestId('apple-signin-button');
        const googleNode = getByTestId('google-signin-button');

        // Depth-first index of each testID in the rendered tree — no `any`,
        // fully typed against ReactTestInstance.
        const order: ReactTestInstance[] = [];
        const walk = (node: ReactTestInstance): void => {
          order.push(node);
          for (const child of node.children) {
            if (typeof child !== 'string') walk(child);
          }
        };
        walk(UNSAFE_root);

        const appleIndex = order.indexOf(appleNode);
        const googleIndex = order.indexOf(googleNode);
        expect(appleIndex).toBeGreaterThanOrEqual(0);
        expect(googleIndex).toBeGreaterThanOrEqual(0);
        expect(appleIndex).toBeLessThanOrEqual(googleIndex);
      });
    });
    ```
    If login.tsx doesn't currently expose `testID="apple-signin-button"` and `testID="google-signin-button"` on the two OAuth button wrappers, add them in the same task — smallest possible change (1 prop per button, no type casts needed). Do NOT reorder the buttons unless Apple is not first (Phase 2 should have already satisfied this; just locking behavior).

    If after inspection Apple is currently BELOW Google, this is a T-7-07 violation — fix it by swapping the button order in login.tsx so that Apple renders first (Guideline 4.8 compliance). Document as a Phase 7 bugfix in the commit message.

    **Step 2 — Run the full validation suite:**
    Run the mobile full test suite and typecheck. Fix any regressions introduced by Tasks 1-6.

    **Step 3 — Manual smoke test map (logged, not executed):**
    Append a `<manual_verification>` section to this plan's SUMMARY.md output:
    - Launch app → see 5 tabs at bottom with teal home active
    - Tap More → see profile card with avatar/nickname/bio preview
    - Tap "프로필 편집" → edit nickname to `테스트` → Save → success toast → name updates in More tab card
    - Tap avatar → ActionSheet appears with 4 options (3 if no custom avatar)
    - Settings → Language → select English → UI immediately switches to English; back to More shows "Edit profile"
    - Settings → Push → toggle → appropriate OS prompt or Alert appears
    - More → "가입한 커뮤니티" row → list renders (or empty state if user has none)
    - More → Log out → Alert with destructive button → tap Log out → navigates to login screen
    - Relaunch app → still logged out

    (This list is for the end-of-phase checkpoint in Plan 07-03 submission — not part of this plan's automated verify.)
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm test:ci 2>&1 | tail -20</automated>
    <automated>cd apps/mobile && pnpm typecheck 2>&1 | tail -10</automated>
    <automated>cd apps/mobile && pnpm lint 2>&1 | tail -10</automated>
    <automated>grep -n "AppleAuthenticationButton\|apple-btn" apps/mobile/tests/auth/login-snapshot.test.tsx</automated>
    <automated>grep -n "appleIndex\|googleIndex" apps/mobile/tests/auth/login-snapshot.test.tsx</automated>
  </verify>
  <done>
    - Login snapshot test passes and asserts Apple-at-or-above-Google
    - login.tsx has testID or accessibilityLabel on both OAuth buttons
    - Full test suite green (pnpm test:ci exits 0)
    - Typecheck clean
    - Lint clean for new/modified files
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile client → Supabase RLS | Profile updates (nickname/bio/avatar/language/dm_launch_notify) cross from untrusted client into server — RLS `profiles_update_own` is the enforcement |
| mobile client → Supabase Storage | Avatar uploads cross from untrusted client; bucket RLS + MIME/size limits enforce per-user folder isolation |
| mobile client → OS (Notifications) | Push permission state owned by OS; client reconciles state but cannot force-grant |
| mobile client → SecureStore | Session tokens stored locally; sign-out MUST wipe |
| mobile client → TanStack cache | Cached data from prior user must never be visible to next user on same device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-03 | Spoofing/Tampering | `hooks/profile/useUpdateProfile.ts` + `profiles` table | mitigate | Client uses `.eq('user_id', user.id)` and authenticated supabase client; server enforces `profiles_update_own` RLS (existing from Phase 1). Test `tests/profile/useUpdateProfile.test.ts` asserts the filter is present. |
| T-7-04 | Tampering/DoS | `hooks/profile/useUploadAvatar.ts` + `avatars` bucket | mitigate | MIME allowlist `image/jpeg|png|webp` enforced at bucket creation (Plan 07-02 Wave 0 migration); client-side 2MB size check after `manipulateAsync` compression; path enforces `{userId}/avatar-{ts}.jpg` so bucket RLS scopes writes to self. Test asserts both checks. |
| T-7-05 | Info Disclosure / Session | `stores/authStore.ts signOut` + queryClient | mitigate | `signOut` clears state AND `queryClient.clear()` inside `finally` block so offline/error logout still wipes cached data. Test `tests/auth/signOut.test.ts` has a dedicated assertion for the finally-block guarantee. SecureStore token cleared by `supabase.auth.signOut`. |
| T-7-07 | Compliance / Security Gate | `app/(auth)/login.tsx` | mitigate | Login snapshot test asserts Apple Sign-In renders at-or-above Google. Violation blocks Phase gate. Pitfall 4 documented; Plan 07-03 re-runs snapshot pre-submission. |
</threat_model>

<verification>
End-of-plan verification (run after Task 7):

```bash
# Type safety
cd apps/mobile && pnpm typecheck

# Full test suite — Wave 0 stubs skipped, new/filled tests green
cd apps/mobile && pnpm test:ci

# Lint clean
cd apps/mobile && pnpm lint

# Grep gates for key pattern presence
grep -E "name=\"(shop|dm|more)\"" apps/mobile/app/\(tabs\)/_layout.tsx
grep "href: null" apps/mobile/app/\(tabs\)/_layout.tsx
grep "dm_launch_notify" packages/db/src/schema/auth.ts
grep "queryClient.clear" apps/mobile/stores/authStore.ts
grep "프로필 편집" packages/shared/src/i18n/locales/ko/more.json
grep "chatbubbles-outline" apps/mobile/app/\(tabs\)/_layout.tsx
```

All of the above must return non-empty output / exit 0.
</verification>

<success_criteria>
- MORE-01: profile-edit.tsx exists and allows nickname/bio/avatar editing with single Save button; useUpdateProfile + useUploadAvatar hooks exist with test coverage.
- MORE-02: Language change in /(more)/language triggers immediate i18n.changeLanguage + persists via useUpdateProfile; tests pass.
- MORE-03: useMyCommunities hook queries community_members + join + maps result; joined-communities.tsx renders list with loading/empty states.
- MORE-04: settings.tsx exposes Language / Push / Per-community / Terms / Privacy / Version rows; push switch reconciles with OS permission state.
- MORE-05: authStore.signOut() clears state + queryClient cache + SecureStore (via supabase.auth.signOut); More tab Alert routes to /(auth)/login on confirm.
- 5-tab navigation visible (Home / Community / Shop / DM / More) with notifications hidden via href:null.
- T-7-03, T-7-04, T-7-05, T-7-07 controls in place with grep/test verification.
- All REQ IDs (MORE-01..05) covered by at least one task.
- No TypeScript errors; full vitest suite exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/07-launch-polish/07-01-SUMMARY.md` documenting:
- Artifacts created/modified per task
- Test files promoted from `it.todo` → real tests (4 of 7; remaining 3 are completed by other plans or this plan's tasks 5-7)
- Key decisions made during implementation (e.g., queryClient extraction location, Application.nativeApplicationVersion vs app.json fallback)
- Threat controls verified via grep/test
- Known follow-ups owned by Plan 07-02 (Shop/DM screens) and Plan 07-03 (legal URL hardcoding replacement)
- Manual smoke test steps (saved for end-of-phase checkpoint)
- Which `07-REVIEWS.md` concerns were addressed (see <review_response> below)
</output>

<review_response>
<!-- Cross-AI review (Codex) concerns from 07-REVIEWS.md — how each was addressed in 07-01-PLAN.md: -->

- **HIGH — Route ordering (shop/dm routes missing when 5-tab declared):** ADDRESSED — inserted new Task 2b that creates minimal placeholder `apps/mobile/app/(tabs)/shop.tsx` and `apps/mobile/app/(tabs)/dm.tsx` (centered "Coming soon" labels, no hooks) BEFORE Task 3 wires `Tabs.Screen name="shop"` + `name="dm"`. Plan 07-02 Tasks 3 + 4 overwrite these placeholders with production content. Hard dependency documented in Task 2b + Plan 07-02 task ordering.

- **MEDIUM — ATT dependency (expo-tracking-transparency installed-but-unused):** ADDRESSED — Task 1 install step removed. No `expo-tracking-transparency` in package.json dependencies. No vitest mock for it. Plan 07-03 Task 6 continues to assert the plugin is absent from `app.json` (consistency check). Decision logged in 07-01 Task 1 action block + verify gate.

- **MEDIUM — i18n scope (5-language constraint violated):** ADDRESSED — Task 1 now creates 10 JSON files total (ko+en+th+zh+ja × more+settings) with machine-translated starter copy marked for community review in v1.0.1. Fallback chain changed from `fallbackLng: 'ko'` to `fallbackLng: 'en'` per review suggestion (EN understood more widely than KO by TH/ZH/JA users).

- **MEDIUM — Dependency cycle (authStore ↔ queryClient direct import):** ADDRESSED — Task 6 refactored. `authStore.signOut` no longer imports `queryClient` directly. Added `onSignOut` callback slot + `registerOnSignOut()` registration method. Root layout (`app/_layout.tsx`) registers `() => queryClient.clear()` via a `useEffect`. Integration test confirms end-to-end cache clearing still works (T-7-05 invariant preserved).

- **LOW — Push OFF UX:** ADDRESSED — Task 5 action block + behavior + done acceptance updated. ON→OFF press now shows confirm Alert + opens system settings via `Linking.openSettings()`; switch state does NOT optimistically flip; `useFocusEffect` re-reads `Notifications.getPermissionsAsync()` on return so state always reflects OS truth. Test added for "ON→OFF shows Alert and keeps switch ON until OS state changes".
</review_response>
</output>
